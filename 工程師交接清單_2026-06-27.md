# 英語單字王 MVP 工程師交接清單

日期：2026-06-27  
專案：新港國小英語單字王網站  
Repository：https://github.com/hs5743/english-word-king  
Production Frontend：https://hs5743.github.io/english-word-king/  

## 現況摘要（2026-07-13）

- 目前開發版本：`v1.18.0-CP37`；Supabase Project Ref：`tzvnyluqommusppbzyiy`。
- 正式出題只使用 705 字、2,115 句人工核准題庫；不以 AI 即時生成學生題目。16 階寶石為學習進度，題庫難度獨立為 L1–8。
- 教師與跨校競賽採固定題目包：指定單字保證納入，其餘題數依所選難度隨機補足；題庫外單字與超量指定會被阻擋。
- CP37 本輪：教師題型配置顯示實際題數並移除無差異的「複習檢核」；跨校賽改用自由 `session_title`，取得場次碼者皆可參加；學生與教職員加入安全編輯；學習分析維持規則式統計，增加錯字涉及人數、樣本提示與複製功能。
- CP37 主提交 `907a749` 已推送至 `main`；遷移 `20260713000001_cp37_session_title.sql` 與 `admin-api` 已部署。正式站回讀為 `v1.18.0-CP37`，遠端 REST 已確認 `session_title` 欄位可查詢。
- Email 是 Google 登入及資料關聯識別，不允許直接編輯；學生修改同步 `student_roster` 與已建立的 `students` 資料。管理者角色只有超級管理員可以修改。
- 管理端「效能監控日誌」維持原狀，另排後續階段處理。
- 使用者既有的 `英語單字王_單字庫_2026-07-09.csv` 刪除狀態必須保留，不得還原或納入提交。
- 本文件保留完整歷史供追溯；後續接手請先讀本摘要與最上方最新版本紀錄，不必逐段載入舊紀錄。

## 交接目標

請下一位工程師在 3 小時內，依照目前架構與方向繼續開發，不要重做架構、不改變平台方向。此專案目前定位是：

- 前端：GitHub Pages 靜態網站
- 後端：Supabase Auth、Postgres、RLS、Edge Functions
- 主要出題：完整 JSON 核准題庫與確定性演算法
- AI：僅保留管理端明確啟動的模型比較測試，不用於正式學生出題

三小時任務結束前，請務必更新本文件最後的「接手工程師工作紀錄」與「交還檢核表」，讓下一輪開發可以無縫接續。

## 2026-07-12 CP35 題庫內容安全改版（已發布）

- 正式挑戰內容方向改為優先使用已核准題庫例句，避免執行時生成造成單字、英文例句與中文翻譯錯配。
- `data/vocabulary.json` 已完成 Content Schema v2 遷移；705 個啟用單字各有 3 句由 Codex 直接審查的國小適齡例句，共 2,115 句。
- 每句保存英文、繁體中文、目標詞形、唯一 ID、內容版本與核准狀態；不需要呼叫外部 AI 生成或審查。
- 審查來源保存在 `data/content-reviewed-cp35.json`，共 36 批，可由 `npm run apply:reviewed-content` 重建套用結果。
- `supabase/functions/generate-challenge/index.ts` 本機版本已調整為：有三句核准例句時隨機選用其中一句；尚未部署 Edge Function。
- 正式學生挑戰強制使用 `reviewed_bank`，不再優先呼叫 AI 即時造句；AI 金鑰僅保留給管理端明確啟動的模型比較測試。
- 句子題新增 `answerWord`，會使用核准例句實際保存的目標詞形（例如複數或動詞變化），熟練度仍記錄在基本單字，避免填空答案與句子文法不合。
- 教師自訂單字若不在核准題庫中會被忽略，不再套用 `I can use ... in class.` 的不可靠通用例句。
- 新增 `npm run audit:content:strict`。2026-07-12 驗證結果：705 個啟用單字、2,115 句、2,115 句核准、0 未核准、0 稽核問題。
- 額外驗證：例句 ID 無重複、每個單字的三句英文無重複、必要欄位無缺漏。
- 原資料中的 `Expansion` 是分類標題而非學習單字，維持停用，不列入 705 個啟用詞。
- `npm run check`、`npm run build:pages`、`git diff --check` 均已通過；`dist/` 已可正確產出。
- CP35 題庫提交 `4952ff2` 與出題安全修補 `82bd1c6` 已推送至 `main`；Supabase `generate-challenge` 已重新部署。
- 正式遠端回讀：GitHub Pages 為 705 個啟用單字、2,115 句、2,115 句核准，`js/config.js` 顯示 `v1.16.0-CP35`。
- 正式 Supabase 出題 API 已連續測試 3 輪共 36 題：來源均為 `reviewed_bank`，36/36 題的單字、英文例句、中譯與 `answerWord` 都能精確回查至公開核准題庫，且填空位置完整。
- 使用者既有的 CSV 刪除狀態必須保留，不得還原或納入 CP35 改版。

## 2026-07-12 CP35.1 介面一致性修補

- 學生挑戰轉場由「正在呼叫 AI 生成題目中」改為「正在從核准題庫準備英語挑戰」，與 `reviewed_bank` 正式機制一致。
- 首頁原石開鑿工具每次開啟時，會從該單字三句已核准例句中隨機選定一組；英文、中譯、語音辨識與評分全程使用同一句。舊資料才回退至 legacy `sentence`。
- `join.html` 三大挑戰模式移除不準確的「AI 即時評分／AI 生成情境」，改為「語音辨識即時回饋／核准例句・中文提示」。
- `join.html`、`class.html`、`app.html`、`teacher.html`、`admin.html`、`scoreboard.html` 統一提供 44px 以上的 Material Symbols「返回大廳」按鈕，手機顯示精簡為「大廳」。
- 管理端「AI 出題效能與統計日誌」本輪依使用者指示維持原狀；除了管理頁頂部新增返回大廳按鈕外，不修改該面板的標題、統計或來源標籤，留待下一階段決定。
- 正式版本更新為 `v1.16.1-CP35`；發布後須回讀版本、六頁導覽、登入頁文案及正式題庫數量。
- Commit `eb23043` 已推送至 `main` 並完成 GitHub Pages 發布；正式站回讀確認六頁導覽、新文案、開鑿例句隨機邏輯與行動版導覽均已上線。
- 正式公開題庫再次確認為 705 個啟用單字、2,115 句核准例句；管理端效能日誌原有文字與判定邏輯未改動。

## 目前已完成架構

### 平台與部署

- GitHub Pages 已設定為正式前端部署方式。
- GitHub Actions workflow 會在 push `main` 後自動打包並部署。
- Supabase 已建立正式專案：
  - Project URL：`https://tzvnyluqommusppbzyiy.supabase.co`
  - Project ref：`tzvnyluqommusppbzyiy`
- Supabase SQL upgrade 已由使用者在 SQL Editor 套用成功。
- Supabase Edge Functions 已部署：
  - `generate-challenge`
  - `admin-api`
- `supabase/config.toml` 中 `generate-challenge` 與 `admin-api` 已設定 `verify_jwt = false`，但 function 內部仍會自行檢查使用者 JWT 與角色。

### 前端主要頁面

- `index.html`：首頁、大廳、排行榜入口。
- `join.html`：學生每日挑戰入口。
- `class.html`：新增的課堂挑戰學生入口。
- `app.html`：學生挑戰主畫面。
- `teacher.html`：教師開課與即時排行榜。
- `admin.html`：管理員後台。

### 角色入口設計

目前已改成角色分流：

- 學生每日挑戰：`join.html`
- 學生課堂挑戰：`class.html`
- 教師：`teacher.html`
- 管理員：`admin.html`

不要再把教師或管理員登入塞回學生入口，使用者已明確希望角色入口分開。

## 目前已完成功能

### 學生端

- Google OAuth 登入。
- 只允許 `@gapp.hcc.edu.tw` 網域。
- 登入後會檢查 `student_roster`。
- 若名冊內學生尚未存在於 `students`，會自動建立學生 profile。
- 支援三種模式：
  - 每日挑戰：每日計分一次。
  - 自由練習：不計每日分數，但更新 mastery。
  - 課堂挑戰：不先產生每日題目，完成後寫入 `daily_attempts.session_id` 供教師排行榜使用。
- 完成頁排行榜連到 `index.html#leaderboard`。

### 教師端

- 教師可登入教師看板。
- 可建立 6 位數課堂碼。
- 可關閉課堂場次。
- 可看課堂即時排行榜。
- 教師頁已提示學生使用 `class.html` 加入課堂挑戰。

### 管理員端

- 管理員可登入後台。
- 管理 API 透過 Edge Function `admin-api` 執行敏感操作，不直接讓前端寫 `system_config` 等敏感表。
- 可管理學生名冊。
- 可管理角色。
- 可設定 AI key。
- 已新增 AI key 狀態檢查與 AI 出題測試功能。

### 出題與題庫

目前有兩個題庫檔：

- `data/vocabulary.json`
- `data/sentence-patterns.json`

目前資料量：

- vocabulary 約 706 個單字。
- sentence patterns 約 687 筆句型。

## 交還前必填：接手工程師工作紀錄

## 2026-07-11 CP34 v1.15.2 AI 例句中英配對與詞義修正

- [x] 定位 `front`、`back` 錯配原因：AI 英文例句通過、AI 中文翻譯不合格時，舊流程保留 AI 英文卻套入題庫另一情境的中文。
- [x] `normalizeChallenge()` 的首次題目與重複題目均改成英文例句、中文翻譯成對採用；任一方未通過驗證即整組回退題庫。
- [x] `ball` 位於 Sports 主題且例句皆為球類情境，中文由「球/舞會」修正為「球」。
- [x] `npm.cmd run check`、`audit:sentences`、`audit:vocab`、`build:pages` 與 `git diff --check` 通過。
- [x] `generate-challenge` Edge Function 已部署至 Supabase 正式專案。
- [x] 版本更新為 `v1.15.2-CP34`，已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。
- [x] 已載入的舊挑戰不會被追溯修改；學生需重新產生一組挑戰才能取得修正後題目。

## 2026-07-11 CP34 v1.15.1 語音介面與辨識穩定性修正

- [x] 移除重複的「英文口音」選單，改為清晰美式、溫和美式、沉穩英式三選一；音色會自動帶入相應語系。
- [x] 修正語速按鈕 inline style 導致快速與標準可能同時呈現黃色選取狀態，任何時候只保留一個 active 狀態。
- [x] Web Speech API 已要求最多 3 個候選但舊流程只取第一筆；改為依題目文字、目標單字、編輯距離及信心值挑選最合理候選。
- [x] 針對 `lunch / launch`、`I say drum / I say draw`、`book / look` 建立候選選取測試並通過。
- [x] 將「AI 音標弱項診斷／發音分數」改為「辨識結果與練習建議／辨識參考分數」，明確說明這是受裝置、網路與環境影響的瀏覽器語音轉文字結果。
- [x] 修補版號更新為 `v1.15.1-CP34`，已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

## 2026-07-11 CP34 學生端英語示範語音體驗升級

### 本次方向

- 參考英語會話示範專案，以瀏覽器原生 Web Speech API 完成口音、音色偏好與真正有感的教學慢速，不導入付費雲端 TTS。
- 保留既有 `SpeechEngine.speak(text, lang, rate)` 呼叫相容性，不修改 Supabase schema、登入、挑戰資料或公開功能流程。

### 本次完成事項

- [x] `js/speech.js` 建立可持久化的語音偏好，支援美式／英式及「清晰美式、溫和美式、沉穩英式」三種學生友善音色。
- [x] 依語系、自然／神經語音名稱與常見 Google、Microsoft、Apple 音色自動排序；指定語音不存在時安全退回其他英文聲音。
- [x] 教學慢速句子依標點及每四個單字分段，段落間加入 260ms 停頓；單字保持完整發音，不錯誤拆成字母。
- [x] 新增可取消的朗讀序列，連續點擊時會中止舊內容，避免語音重疊。
- [x] `app.html` 新增寶石學院風格「英語示範聲音」Modal，可選口音、音色並試聽標準／教學慢速；手機仍保留設定入口。
- [x] 首頁設備檢測新增示範聲音輸出檢查，可直接比較標準速度與教學慢速。
- [x] 挑戰、答題回饋、例句與寶石手冊朗讀改為尊重全域語音偏好。
- [x] 設定保存在 `localStorage` 的 `ewk-speech-preferences-v1`，不涉及學生個資或雲端資料。
- [x] 版本更新為 `v1.15.0-CP34`。

### 驗證與發布

- [x] `npm.cmd run check`、`npm.cmd run build:pages` 與 `git diff --check` 通過。
- [x] 未登入開啟 `app.html` 仍會依既有驗證安全返回 `join.html`，未改變登入流程。
- [x] CP34 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 相容性與後續

1. 實際聲音名稱與自然度取決於 Windows、Chrome、Edge、ChromeOS 或 iPad 已安裝的語音；找不到指定口音時會自動降級，不會阻斷挑戰。
2. 請優先蒐集校內主要設備對「標準／教學慢速」差異的感受，再決定是否於 CP35 導入具成本與網路依賴的雲端神經語音。
3. 瀏覽器原生 TTS 無法精準指定開心、鼓勵等情緒曲線；若需跨設備一致的情緒語調，才評估 Supabase Edge Function、音訊快取與雲端 TTS。

## 2026-07-11 CP33 校際對抗賽大螢幕看板改版

### 本次方向

- 將 `scoreboard.html` 從舊版通用淺色看板更新為寶石學院競技場，兼顧投影距離、對比與大型數字辨識。
- 保留 Supabase Realtime、三校計分、個人排行、QR Code、倒數、明暗模式、教師結束比賽與頒獎流程。

### 本次完成事項

- [x] 深藍金色競技場頂欄、正式盾牌、LIVE 狀態與高對比倒數計時。
- [x] 三校總分卡使用獨立寶石徽章、加粗校名、大型分數及三校代表色。
- [x] 個人前三名頒獎台、後續名次、即時事件牆統一為投影友善的高對比卡片。
- [x] 明亮模式仍保留學院背景與深藍品牌頂欄，不再回到灰白通用介面。
- [x] 支援 `prefers-reduced-motion`，版本更新為 `v1.14.0-CP33`。

### 驗證與發布

- [x] `npm.cmd run check`、`npm.cmd run build:pages` 與 `git diff --check` 通過。
- [x] `scoreboard.html` 正式資產與所有既有 DOM ID 保留。
- [x] CP33 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 後續抽查

1. 實際有學生提交時確認分數跳動、前三名與事件牆在投影環境仍清楚。
2. 1920×1080 教室投影建議優先使用深色模式，強光環境再切明亮模式。

## 2026-07-11 CP32 教師端、管理端與閱讀性統一

### 本次方向

- 將 `teacher.html` 與 `admin.html` 同步延伸為寶石學院介面，不修改角色、權限、課堂、報表、名冊、AI 設定或對抗賽功能。
- 依使用者截圖修正寶石手冊已開採卡片的中文翻譯、音標、等級文字對比與字級，將學生閱讀便利及視力保健列為必要驗收條件。

### 本次完成事項

- [x] 教師與管理端加入深藍品牌導覽、正式盾牌、明亮卡片、金藍狀態及一致焦點框。
- [x] 統一表單、按鈕、統計卡、分頁、表格、Modal 與載入遮罩。
- [x] 表格內文提高至至少 `0.88rem` 並增加行高；標題、標籤與輔助文字改為深色高對比。
- [x] 行動版保留主要操作，表格使用可控橫向捲動，避免欄位被壓縮至難以閱讀。
- [x] 寶石手冊加入語意化卡片 class，中文翻譯提高至 `0.9rem`、粗體與深藍文字；音標、等級及開採資訊同步加深。
- [x] 版本更新為 `v1.13.0-CP32`。

### 驗證與發布

- [x] `npm.cmd run check`、`npm.cmd run build:pages` 與 `git diff --check` 通過。
- [x] 使用者要求教師端與管理端一起完成並直接推送部署。
- [x] CP32 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 後續原則

1. 學生介面一般內文不得以低對比淡灰作為主要資訊色；中文翻譯建議至少 `0.9rem`。
2. 教師／管理端表格資料建議至少 `0.88rem`，表頭需有明顯底色與粗體。
3. 新配色需同時檢查美感、對比、字級、行高與行動版可讀性。

## 2026-07-11 CP31 學生 Modal 與寶石手冊視覺統一

### 本次方向

- 統一首頁學生 Modal、英語寶石開採手冊、原石複習工具及挑戰中心的寶石等級／升級 Modal。
- 保留所有 DOM ID、資料渲染、錄音、語音辨識、關閉與升級流程。

### 本次完成事項

- [x] 首頁等級、設備檢測、手冊與開採 Modal 改為明亮高對比學院卡片。
- [x] 統一遮罩、圓角、標題列、關閉按鈕、焦點框、狀態色與行動版高度。
- [x] 寶石手冊分頁新增 `is-active` 與 `aria-selected` 狀態，視覺與輔助語意同步。
- [x] 動態寶石卡片、空狀態與開採輸入套用淺色卡片與可讀文字。
- [x] 挑戰中心等級 Modal 與升級 Modal 套用相同的寶石學院元件語彙。
- [x] 標準操作圖示改用 Material Symbols；礦物本體仍保留資料來源中的寶石 Emoji。
- [x] 版本更新為 `v1.12.0-CP31`。

### 驗證紀錄

- [x] `npm.cmd run check`、`npm.cmd run build:pages` 與 `git diff --check` 通過。
- [x] 發布工具包含更新後的首頁、挑戰中心與共用 CSS／JavaScript。
- [ ] 登入後手冊真實單字資料與升級 Modal 仍需在正式站持續觀察。
- [x] 使用者已要求本輪完成後直接發布。
- [x] 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 下一步

1. 正式站登入後抽查手冊的「開採中／已開採」資料卡與原石複習流程。
2. 發現高、中優先視覺問題時以 CP31 修正版處理。
3. CP32 開始延伸 `teacher.html`。

## 2026-07-11 CP30 學生挑戰主畫面視覺延伸

### 本次方向

- 將 `app.html` 的學生實際作答介面延伸為 CP27–CP29 的寶石冒險學院設計。
- 僅改變視覺與響應式樣式；題型切換、計分、口說辨識、課堂模式、進度、完成頁與資料寫入邏輯不變。

### 本次完成事項

- [x] 頂部列改為深藍學院導覽並使用正式盾牌品牌資產。
- [x] 每日／自由／課堂模式及拼寫／口說／句型分頁改為一致的圖示與高對比狀態。
- [x] 題目卡、進度條、拼字磁磚、輸入框、回饋、導覽按鈕及學習紀錄側欄改為明亮學院元件。
- [x] 完成畫面使用競賽寶石盾牌與 Material Symbols，保留原有統計及按鈕功能。
- [x] 行動版簡化頂部資訊，保留學生、分數與主要操作，模式列可橫向捲動。
- [x] 支援 `prefers-reduced-motion`，版本更新為 `v1.11.0-CP30`。

### 驗證紀錄

- [x] `npm.cmd run check`、`npm.cmd run build:pages` 與 `git diff --check` 通過。
- [x] 本機 `app.html` HTTP 回應 200。
- [x] 未登入瀏覽 `app.html` 會依既有安全流程轉回 `join.html`，登入保護未被繞過或修改。
- [ ] 登入後完整題目畫面與三種題型的瀏覽器視覺 QA 改於正式站登入後持續確認。
- [x] 本機 OAuth 回呼會轉至雲端頁面；使用者已知悉並同意先提交部署，後續發現差異再修正。
- [x] CP30 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 下一步

1. 從正式網站登入，依序檢查拼寫、口說、句型及完成頁。
2. 發現 P0–P2 視覺差異時另開修正版本，不回滾 CP30 的設計基礎。
3. CP31 統一學生 Modal 與寶石手冊。

## 2026-07-11 CP29 學生課堂挑戰入口視覺延伸

### 本次方向

- 延續 CP27 首頁與 CP28 每日挑戰入口，將 `class.html` 改造為「課堂任務通行證」。
- 不修改課堂碼格式、登入判定、`pendingClassCode`、URL 參數解析或 `app.html?mode=class&code=...` 跳轉邏輯。

### 本次完成事項

- [x] 套用深藍學院導覽、正式盾牌、學院背景與金藍高對比元件。
- [x] 桌面版建立課堂說明與通行證雙欄；窄版改為單欄。
- [x] 以競賽寶石盾牌呈現課堂挑戰識別，並加入三步驟說明。
- [x] 課堂碼輸入維持數字過濾、六位限制、Enter 提交及錯誤狀態。
- [x] 主要按鈕高度 58px、輸入框高度 70px，支援鍵盤焦點與 `prefers-reduced-motion`。
- [x] 版本更新為 `v1.10.0-CP29`。

### 驗證紀錄

- [x] `npm.cmd run check` 與 `npm.cmd run build:pages` 通過。
- [x] 本機 `class.html` HTTP 回應 200，頁面圖片全部載入且無水平溢位。
- [x] 輸入 `12a3` 會自動過濾為 `123`。
- [x] 非六位碼提交會顯示「請輸入老師提供的 6 位數課堂碼。」。
- [x] 瀏覽器主控台無錯誤。
- [x] 使用者已確認 `class.html` 視覺並授權正式發布。
- [x] 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 下一步

1. CP29 已發布完成。
2. CP30 開始改造 `app.html` 學生挑戰主畫面，保留所有題型、計分、口說與完成頁功能。

## 2026-07-11 CP28 學生每日挑戰入口視覺延伸

### 本次方向

- 以已發布的 CP27 首頁為唯一設計基礎，將 `join.html` 從深色玻璃星空介面改為明亮的寶石冒險學院學生入口。
- 僅調整視覺、資訊層級與響應式排列；Google OAuth、Supabase 學生名冊查詢、學校選擇、登入狀態及 redirect 邏輯均保持不變。

### 本次完成事項

- [x] 導覽列改為 CP27 深藍學院樣式，沿用正式寶石盾牌品牌資產。
- [x] 桌面版建立左側學院導引、右側登入任務卡的雙欄結構；窄版自動改為單欄。
- [x] 主標題、學校徽章、表單、Google 登入按鈕與提示訊息套用金藍高對比設計。
- [x] 三大挑戰模式使用 CP27 真實寶石圖片，移除主要介面的 Emoji 裝飾。
- [x] 保留 `schoolSelect`、`btnGoogleLogin`、四種 UI state 與所有既有 JavaScript 流程。
- [x] 支援 `prefers-reduced-motion`，主要操作按鈕高度大於 44px。
- [x] 版本更新為 `v1.9.0-CP28`。

### 驗證紀錄

- [x] `npm.cmd run check` 通過。
- [x] `npm.cmd run build:pages` 通過，`dist/join.html` 與共用圖片資產均成功產出。
- [x] 本機 `join.html` HTTP 回應 200。
- [x] 瀏覽器桌面視覺檢查通過，圖片全部載入、頁面無水平溢位。
- [x] 學校選擇可由新港切換至鳳岡，`value` 與 `data-school` 同步更新。
- [x] 瀏覽器主控台無錯誤。
- [x] 使用者已確認 `join.html` 視覺並授權正式發布。
- [x] 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 下一步

1. CP28 已發布完成。
2. CP29 優先延伸 `class.html`，重用 CP28 的學生入口導覽、登入卡與狀態元件。
3. 完成 `class.html` 後再處理學生挑戰主畫面 `app.html`。

## 2026-07-11 CP27 寶石冒險學院首頁正式改版

### 問題背景

- 現有首頁雖已具備完整功能，但色彩層級偏淡、玻璃卡片與 Emoji 風格較不統一，三校競技與寶石成就的品牌特色不夠明確。
- 本輪以已選定的「寶石冒險學院」設計稿為唯一視覺母版，僅調整首頁視覺與介面層級，不改變原有功能。

### 本次完成事項

- [x] 建立 CP27 深藍、寶石藍、金色與三校代表色的首頁設計 Token。
- [x] 建立實際學院場景與寶石盾牌圖片資產，正式納入 `assets/gem-academy/`。
- [x] 依視覺複核結果重新精修主標題，移除厚重描邊，改為較細緻的金色與寶石藍層次。
- [x] 建立四枚透明背景寶石學院特色徽章，取代平台特色卡片原本的單色系統圖示。
- [x] 重構深藍導覽列、學院首屏、主要操作按鈕、即時動態、三校積分、個人排行榜、平台特色與 CTA。
- [x] 三校積分列表會依真實總分重新排序與標示名次，資料來源與 Supabase 查詢不變。
- [x] 以 Material Symbols Rounded 統一標準操作圖示，並將首頁主要區塊的混用 Emoji 改為一致圖示系統。
- [x] 完成 390px 行動版重排、導覽簡化、無水平溢位與 `prefers-reduced-motion` 支援。
- [x] 保留 `js/index.js` 依賴的所有 DOM ID、連結、Modal 與資料介面。
- [x] 發布工具已加入 `assets/` 目錄，版本更新為 `v1.8.0-CP27`。

### 本次修改檔案

- [x] `index.html`
- [x] `css/style.css`
- [x] `js/index.js`
- [x] `js/config.js`
- [x] `tools/build-pages.mjs`
- [x] `assets/gem-academy/hero-academy.png`
- [x] `assets/gem-academy/crest.png`
- [x] `assets/gem-academy/feature-adaptive.png`
- [x] `assets/gem-academy/feature-speech.png`
- [x] `assets/gem-academy/feature-daily.png`
- [x] `assets/gem-academy/feature-competition.png`
- [x] `design-qa.md`
- [x] `工程師交接清單_2026-06-27.md`

### 驗證紀錄

- [x] `npm.cmd run check` 通過（JavaScript、inline scripts、JSON）。
- [x] `npm.cmd run build:pages` 通過，`dist/assets/gem-academy/` 成功產出。
- [x] 首頁、學院背景與盾牌資產的本機 HTTP 檢查均回傳 200。
- [x] 29 個關鍵 DOM ID 與必要連結介面檢查通過。
- [x] 已用桌面 1440×900 與行動 390×844 取得瀏覽器畫面並進行視覺比對。
- [x] 390px 行動版 `documentElement.scrollWidth <= innerWidth`，無水平溢位。
- [x] 使用者已確認首頁視覺並授權正式發布。
- [x] 發布前瀏覽器重新載入後無新增主控台錯誤。
- [x] 已提交並推送 `main`，由 GitHub Actions 執行 GitHub Pages 發布。

### 交還 commit / branch

- Branch：`main`
- Commit：CP27 首頁正式改版發布 commit（以 `git log -1` 為準）
- 是否已 push：是

### CP28 起的介面延伸順序

後續頁面統一沿用 CP27 的深藍、寶石藍、金色、三校代表色、實體寶石資產及高對比卡片語彙；只調整視覺與操作層級，不任意改動既有資料介面或功能流程。優先順序如下：

1. **學生每日挑戰入口 `join.html`**：先統一登入、挑戰模式、學生狀態與錯誤提示的視覺。
2. **學生課堂挑戰入口 `class.html`**：延伸課堂碼輸入、場次狀態與加入流程，保持與每日挑戰一致。
3. **學生挑戰主畫面 `app.html`**：重整題目卡、答題回饋、口說辨識、進度與完成頁；功能及計分邏輯不變。
4. **學生相關 Modal 與寶石手冊**：統一卡片、徽章、狀態色、焦點與行動版操作。
5. **教師端 `teacher.html` 與看板 `scoreboard.html`**：完成學生端後再延伸。
6. **管理端 `admin.html`**：最後處理，以資訊密度、表格可讀性與操作安全為優先。

每一頁仍須依序完成桌面／390px 行動版、鍵盤操作、瀏覽器主控台、`npm.cmd run check` 與 `npm.cmd run build:pages` 驗證，再進入下一頁。

## 2026-07-10 CP26 校際對抗賽管理權移轉與大螢幕看板

### 本次工作時間

- 開始時間：2026-07-10 15:20
- 完成時間：2026-07-10 16:00

### 問題背景

- 原本三校對抗賽缺乏專屬的大螢幕看板投影頁面，無法為大賽或聯合比拼提供足夠的遊戲化互動氛圍。
- 為了解決功能混淆、易出錯以及首頁直播橫幅被日常班級挑戰賽干擾的問題，將「三校即時對抗賽」的管理控制權完全收歸至管理員（admin）後台，與普通教師個人的日常「班級挑戰賽」作介面與角色上的徹底隔離。

### 本次完成事項

- [x] **資料庫與權限設定 (Database Migrations)**：
  - 新增 `supabase/migrations/20260710000000_scoreboard_anon_policy.sql` 遷移檔並將 RLS Policy 寫入 `production-upgrade-existing-project.sql`。
  - 在 `challenge_sessions` 資料表新增 `session_type` 欄位以區分 `'class'` 與 `'contest'`。
  - 新增 RLS Policy 允許未登入與已登入用戶讀取進行中的對抗賽（`session_type = 'contest'`）基本資訊以供大廳展示。
- [x] **大螢幕看板頁面開發 (Scoreboard Page)**：
  - 建立全新 `scoreboard.html` 頁面，採用深太空投影背景與毛玻璃發光卡片（Xingang Gold, Fenggong Blue, Fengtian Green）。
  - 實作 Supabase Realtime 訂閱，學生提交時以 Countup 動畫滾動累加積分，並自動刷新 Top 30 排行榜與提交動態牆。
  - 實作 Web Audio API 音效合成（triplet chord 與 victory sliding synth-beep 聲效）與 Confetti 彩帶雨和頒獎台特效。
- [x] **管理員對抗賽管理控制台**：
  - 在 `admin.html` 中新增「🏆 校際對抗賽管理」分頁標籤與面板，僅管理員有權建立或結束對抗賽場次、開啟大螢幕看板。
  - 建立的對抗賽會被設為 `session_type = 'contest'`，且 `school = null`，以允許三校學生跨校加入挑戰。
- [x] **學生端跨校加入處理**：
  - 修改 `js/challenge.js`，若 `session_type === 'contest'` 則繞過學校檢查，允許三校學生跨校加入挑戰。
- [x] **大廳直播橫幅提示**：
  - 修改 `index.html` 與 `js/index.js`，在大廳首頁新增對抗賽進行中的直播橫幅，引導觀戰。
- [x] **動態參賽 QR Code 與連結功能**：
  - 在 `scoreboard.html` 的左方學校面板底部新增了帶有 6 位數字代碼的動態 QR Code 區塊與加入連結，方便台下學生即時掃碼加入；同時在 `admin.html` 進行中卡片也同步展示參賽連結與 QR Code。
- [x] **打包與版本管理**：
  - 將 `scoreboard.html` 加入 `tools/build-pages.mjs` 發布數組中，確保編譯產出。
  - 更新版本號為 `v1.7.0-CP26`，並通過 `npm.cmd run check` 及 `build:pages` 打包檢查。

### 本次修改檔案

- [x] `admin.html`
- [x] `index.html`
- [x] `js/challenge.js`
- [x] `js/config.js`
- [x] `js/index.js`
- [x] `scoreboard.html`
- [x] `tools/build-pages.mjs`
- [x] `supabase/migrations/20260710000000_scoreboard_anon_policy.sql`
- [x] `supabase/production-upgrade-existing-project.sql`

---

## 2026-07-09 CP25 全站單字庫中譯自動校對與雙例句升級

### 本次工作時間

- 開始時間：2026-07-09 05:20
- 完成時間：2026-07-09 05:40

### 問題背景

- 原本單字庫的英文翻譯存在部分不精確或錯誤（例如 French 誤植為「薯條」，sister/brother 翻譯為「姊妹/兄弟」不符合台灣國小常用口語）。
- 每個單字僅有一個例句，為了優化每日挑戰時的練習效果，防止每次都使用 AI 生成造成延遲或費用，決定在單字庫中直接為每個單字配置第二個適合國小 3-6 年級程度（校園、家庭、勵志主題）的高品質例句，並補齊標準 IPA 音標。

### 本次完成事項

- [x] **單字庫 706 個單字中譯校對與音標補齊**：
  - 逐字審查並優化了 521 個單字的中譯（如更正 French、sister、brother 等），使其完全契合台灣在地小學口語。
  - 補齊所有 706 個單字的美式標準 IPA 音標。
- [x] **第二高品質例句生成**：
  - 為所有 706 個單字生成了字數限制 6-15 字、語法句型符合 3-6 年級程度、融入校園與家庭正向勵志學習生活的第二例句與中文翻譯。
- [x] **出題後端 Deno Edge Function 升級**：
  - 修改 `supabase/functions/generate-challenge/index.ts` 中的 `toFallbackWord` 與 `toChallengeItem`，新增對 `phonetic`、`sentence2`、`sentence2Zh` 的支援。
  - 實作了雙例句隨機選用出題機制（以 50% 機率隨機選用第一或第二例句），大幅提昇學生的練習變化度與題庫品質。
- [x] **單字庫自動升級維護工具開發**：
  - 新增 `tools/apply-csv-upgrade.mjs`，方便未來工程師直接透過 CSV 自動更新與轉換寫入 `data/vocabulary.json` 與發佈區 `dist/data/vocabulary.json`。
- [x] **隱藏練習介面的音標顯示**：
  - 因小學階段未用到音標，在 `app.html` 的 CSS 樣式中設定隱藏 `.word-phonetic` 與 `.speech-phonetic`，並在 `js/challenge.js` 中移除答題揭曉區域的音標串接，以防混淆。
- [x] **修正首頁文字以精準描述功能**：
  - 移除「AI出題、口說診斷」等字眼，更正為「適性化智慧出題、口說發音練習」，精準呈現現有機制。
- [x] **順利完成發佈與線上部署**：
  - 執行並通過了 `npm run check` 與 `npm run build:pages`。
  - 已將所有代碼合併至 `main` 並推送至 GitHub，同時透過 Supabase CLI 將 `generate-challenge` 函數成功部署上線。

### 本次修改檔案

- [x] `data/vocabulary.json`
- [x] `supabase/functions/generate-challenge/index.ts`
- [x] `data/translation_review_report.md`
- [x] `data/vocabulary_double_sentences_draft.csv`
- [x] `tools/apply-csv-upgrade.mjs`
- [x] `tools/generate-double-sentences.mjs`
- [x] `index.html`
- [x] `app.html`
- [x] `js/challenge.js`

---

## 2026-07-05 CP24 全站 UI/UX 設計系統升級與美感優化

### 本次工作時間

- 開始時間：2026-07-05 21:48
- 完成時間：2026-07-05 22:00

### 問題背景

- 原本專案的 UI/UX 設計略顯單調且有不一致之處（內聯 Dark 樣式與外部 Light 樣式強行覆寫混雜）。
- 網頁卡片邊框生硬、陰影沉重、按鈕點擊動效不夠活潑，且挑戰完成後的獎盃與統計資訊缺乏遊戲化的精緻美感。

### 本次完成事項

- [x] **建立 HSL 色彩設計 Token 與毛玻璃變數**：
  - 在 `css/style.css` 重新定義色彩系統，全面替換 HEX 色值為 HSL 格式。
  - 新增毛玻璃透明度背景變數 `var(--glass-bg)`、透光細白邊框 `var(--glass-border)` 及輕盈軟陰影 `var(--glass-shadow)` / `var(--glass-shadow-hover)`。
- [x] **卡片與面板毛玻璃化（Glassmorphism）**：
  - 更新全站卡片（`.glass-card` 與 `.challenge-main`）為高呼吸感毛玻璃效果（`backdrop-filter: blur(16px)`），加入輕快陰影，移除灰黑實線邊框。
  - 將右側複習與熟練度側邊欄（`.mastery-sidebar`）與底部抽屜（`.mastery-drawer`）改為統一的玻璃鏡面質感。
  - 優化音波容器與發音圓環，移除多餘的實體邊框，以輕透的陰影與白透框呈現。
- [x] **按鈕物理化回彈與 Q 彈動效（Unified Bounce Interactions）**：
  - 為全站按鈕（`.btn`、`.nav-btn` 等）套用貝氏曲線彈簧動畫變數 `var(--spring-transition)`。
  - 統一按鈕在 Hover 時平滑浮起並微增發光陰影，點擊時下壓（`scale(0.95)`）的物理點壓感，按鈕反應極度流暢生動。
- [x] **答題與完成關卡（WOW Factor）體驗美化**：
  - **浮動獎盃動畫**：將挑戰完成畫面的獎盃（`completion-trophy`）加上極致精美的緩慢上下浮動動畫與金色光暈，極具成就感。
  - **統計數據卡片浮動**：將完成關卡的三個統計卡片（`completion-stat`）加上滑過浮起與微發光陰影，體驗更富趣味。
  - **修正白色分隔線 bug**：將 app.html 內聯樣式中原本隱藏的白色分隔線（`.divider`）在 Light 模式下調整為暗透明度色，使其清晰可見。
  - **寶石等級 Modal 毛玻璃化**：優化硬度等級 Modal（`levelModal`），使其與全站玻璃風輕透亮色完全吻合，極具統一質感。
- [x] **版本與還原點管理**：
  - 本次為重大視覺大改版，將 `APP_VERSION` 自 `v1.5.8-CP23` 提升至大改版版號 **`v1.6.0-CP24`**。
  - **還原點**：在開始前已建立還原點 Git 分支 `backup-before-ui-upgrade` 以確保安全。

### 本次修改檔案

- [x] `css/style.css`
- [x] `app.html`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `cmd /c npm run check` 通過
- [x] `cmd /c npm run build:pages` 通過

### 交還 commit / branch

- Branch：`main`
- Commit Message：Upgrade entire UI/UX to premium glassmorphism light theme (CP24)
- 是否已 push：是

## 2026-07-05 CP23 優化收音狀態標籤全中文化以避免教學與題目混淆

### 本次工作時間

- 開始時間：2026-07-05 01:38
- 完成時間：2026-07-05 01:43

### 問題背景

- 學生或教師在使用口說/錄音功能時，發現麥克風旁在收音時會跳出即時辨識的英文文字（例如收錄雜音或學生詢問聲，顯現 `正在聽：how do you say`）。
- 由於此文字出現在狀態標籤中，容易讓小學生誤以為是「系統指定要唸的句子」，進而與上方的學習字彙或句子混淆，造成教學阻礙。

### 本次完成事項

- [x] **收音過程標籤全中文化**：
  - 修改 `js/challenge.js` 中所有與語音錄音相關的函式（`toggleSpellingRecording`、`toggleRecording`、`toggleSentenceRecording`、`toggleFollowUp`）。
  - 將原本在 startListening 回呼中即時顯示英文內容的 `正在聽：${interim}` 改為中文化的 **`正在收音，請繼續...`**；將 `辨識結果：${interim}` 改為中文化的 **`已收到聲音，正在評分...`**。
  - 主口說題型（Questions 4-7）中，即時辨識的英文內容仍會保留在專門的 `liveText`（即時辨識結果白框）中呈現，但狀態行維持乾淨的中文字句，確保功能性與防混淆取得良好平衡。
- [x] **更新版本號**：
  - 將 `js/config.js` 的 `APP_VERSION` 自 `v1.5.7-CP22` 提升至 `v1.5.8-CP23`。

### 本次修改檔案

- [x] `js/challenge.js`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `cmd /c npm run check` 通過
- [x] `cmd /c npm run build:pages` 通過

### 交還 commit / branch

- Branch：`main`
- Commit Message：Localize speech recognition status labels during recording to Chinese (CP23)
- 是否已 push：是

## 2026-07-05 CP22 優化口說題型介里緊湊度與辨識反饋資訊

### 本次工作時間

- 開始時間：2026-07-05 01:15
- 完成時間：2026-07-05 01:25

### 問題背景

- 在口說題中，原先版面垂直間隙與尺寸過大，使「下一步」按鈕和 AI 診斷欄被擠出螢幕之外，必須滾動才能看到，極易讓學生誤以為卡關。
- 此外，原先「短音輔導提示」為灰色小字，很容易被學生忽略，且在辨識失敗時並未清晰顯示「系統聽成什麼」，使學生難以獲知如何糾正發音。

### 本次完成事項

- [x] **介面極致緊湊化（免除滾動）**：
  - 在 `app.html` 新增 `#panelSpeech` 口說面板緊湊化樣式覆寫：縮減整體 Padding，將音波容器高度從 100px 縮小至 **52px**，麥克風按鈕自 88px 縮小至 **68px**，分數環自 80px 縮小至 **68px**。
  - 這確保在常見的平板、Chromebook 等小螢幕上，所有核心資訊與最下方的「下一步」按鈕都能 **100% 完整呈現在單一畫面內，無需滾動**。
- [x] **加強「短音輔助提示」視覺層級**：
  - 修改 `js/challenge.js` 的 `renderSpeechQuestion`，若為短單字，則將提示區塊（`#speechExample`）改為**金色外框與金色背景的醒目提示框**，並大字粗體標註：`💡 短音輔助唸法：I say [單字]`。
- [x] **新增「系統聽成」文字回饋**：
  - 在 `js/challenge.js` 中的 `handleSpeechScore` 音標診斷欄最上方加入 **`系統聽成：[聽到的內容]`** 的清晰字樣。
  - 將下方單字碎片的 Wrong/Close 標籤優化為 `[預期單字] (聽成: [聽到單字])` 的清晰對照（如 `her (聽成: here)`）。這使得發音失敗的學生能一目了然得知發音盲點。
- [x] **更新版本號**：
  - 將 `js/config.js` 的 `APP_VERSION` 自 `v1.5.6-CP21` 提升至 `v1.5.7-CP22`。

### 本次修改檔案

- [x] `app.html`
- [x] `js/challenge.js`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `cmd /c npm run check` 通過
- [x] `cmd /c npm run build:pages` 通過

### 交還 commit / branch

- Branch：`main`
- Commit Message：Optimize speech panel layout and add detailed recognition feedback (CP22)
- 是否已 push：是

## 2026-07-05 CP21 提升口說挑戰使用者體驗與流暢度

### 本次工作時間

- 開始時間：2026-07-05 00:55
- 完成時間：2026-07-05 01:05

### 問題背景

- 學生在挑戰每日挑戰和自由練習時，反應口說部分流程卡頓、容易困惑，且麥克風問題容易導致卡關。
- 主要原因為：單字口說靜音檢測等待時間過長（4.5 秒）、口說單字對了之後又突然彈出例句朗讀的雙重任務讓人困惑、選填與必填加分引導不夠明確，以及短單字在 Web Speech API 容易辨識失敗。

### 本次完成事項

- [x] **縮短口說靜音逾時時間**：
  - 修改 `js/speech.js` 的 `startListening` 函式，支援 `silenceTimeoutMs` 參數，若未傳入且辨識目標單字數 <= 3 時，自動將等待時間由 4.5 秒縮短至 **2.0 秒**，唸完立即返回結果，大幅提升答題節奏。
- [x] **簡化口說題面板**：
  - 調整 `js/challenge.js`，取消口說單字題答對後再額外彈出例句朗讀的二次挑戰，使其聚焦於單字朗讀本身。
- [x] **明確標示選填加分與下一步按鈕引導**：
  - 在 `app.html` 更新拼字和句型題後的口說加分標題，明確標註 `🌟 朗讀單字額外加分區 (選填，可直接按下一步繼續)` 和 `🌟 朗讀句子額外加分區 (選填，可直接按下一步繼續)`。
  - 在 `app.html` 新增 `.nav-btn--next-pulse` 脈衝 CSS 動畫。
  - 當拼字題或句型題答對並揭曉答案（即出現選填口說）時，自動為「下一步」按鈕加入脈衝呼吸燈狀態，引導學生可以直接點選通過。新題目載入時自動清除該狀態。
- [x] **新增短單字失敗引導句提示**：
  - 在口說題若辨識分數未達 80 分且屬於短單字時，自動在 AI 診斷欄位內高亮提示引導句（例如：`💡 如果單字唸不出來，可以試著朗讀整句：I say [單字]`），以輔助語音辨識之成功率。
- [x] **更新版本號**：
  - 將 `js/config.js` 的 `APP_VERSION` 從 `v1.5.5-CP20` 提升至 `v1.5.6-CP21`。

### 本次修改檔案

- [x] `js/speech.js`
- [x] `js/challenge.js`
- [x] `app.html`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `cmd /c npm run check` 通過
- [x] `cmd /c npm run build:pages` 通過

### 交還 commit / branch

- Branch：`main`
- Commit Message：Improve speech challenge user experience and flow (CP21)
- 是否已 push：是

## 2026-07-05 CP20 升級 GitHub Actions 部署工作流與 Actions 版本

### 本次工作時間

- 開始時間：2026-07-05 00:40
- 完成時間：2026-07-05 00:50

### 問題背景

- 使用者收到 GitHub Actions 部署錯誤通知信（`Deployment failed, try again later.`）。
- 原因為 GitHub Actions 棄用 Node.js 20 執行環境並強制所有 JavaScript actions 在 Node.js 24 容器中執行。專案舊有的 `checkout@v4`、`setup-node@v4`、`upload-pages-artifact@v3` 及 `deploy-pages@v4` 行為在 Node 24 下有相容性問題，導致部署偶爾或持續失敗。

### 本次完成事項

- [x] **升級部署工作流 Actions 套件**：
  - `actions/checkout@v4` 升級至 `actions/checkout@v7`
  - `actions/setup-node@v4` 升級至 `actions/setup-node@v6`
  - `actions/upload-pages-artifact@v3` 升級至 `actions/upload-pages-artifact@v5`
  - `actions/deploy-pages@v4` 升級至 `actions/deploy-pages@v5`
- [x] **升級工作流中的 Node.js 執行版本**：
  - 將工作流內的 `node-version: 20` 改為 `24`，與本機環境完全對齊。
- [x] **更新版本號**：
  - `js/config.js` 的 `APP_VERSION` 由 `v1.5.4-CP19` 更新至 `v1.5.5-CP20`。

### 本次修改檔案

- [x] `.github/workflows/pages.yml`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `cmd /c npm run check` 通過
- [x] `cmd /c npm run build:pages` 通過

### 交還 commit / branch

- Branch：`main`
- Commit Message：Upgrade GitHub Actions workflow to run on Node 24 native actions (CP20)
- 是否已 push：是

## 2026-07-04 CP19 AI 造句品質與繁體中文驗證改善

### 本次工作時間

- 開始時間：2026-07-04 09:12
- 完成時間：2026-07-04 09:12

### 問題背景

- 使用者回報 AI 造句有時自然、有時不順，且中文翻譯偶爾出現簡體字或台灣用語不一致。
- 經檢查，畫面上的 `big`、`her`、`than`、`in`、`bird` 範例多半不是本地題庫原句，而是 AI 生成後通過後端驗證。
- 根因不是單一 API key，而是 AI prompt 限制不夠明確、後端驗證未完整阻擋簡體/怪情境、本地題庫另有大量 `needsReview` 待審項目。

### 本次完成事項

- [x] **強化 AI 生成規則**：
  - 在 `generate-challenge` prompt 補入品質規則，要求自然、簡單、適合台灣國小學生的英文例句。
  - 明確要求 `sentenceZh` 必須使用台灣繁體中文，不可出現簡體字或中國用語。
  - 要求代名詞題不可把受詞代名詞誤用成所有格。
- [x] **強化後端驗證**：
  - 新增 `hasDisallowedSimplifiedChinese()`，AI 中文翻譯若出現疑似簡體字，改回退到題庫中譯。
  - 新增英文怪情境阻擋，如 `brand of`、`logo on`、`trademark`、`advertisement` 等，避免類似「麵包品牌上的鳥 logo」這種不自然句子進入題目。
  - 針對 `her` 受詞題，阻擋 `her favorite/book/...` 這類所有格用法。
- [x] **新增句子品質稽核工具**：
  - 新增 `tools/audit-sentence-quality.mjs`，可盤點 `needsReview`、疑似簡體翻譯、可疑英文句型。
  - 在 `package.json` 新增 `npm run audit:sentences`。
- [x] **版本號更新**：
  - 將 `js/config.js` 的 `APP_VERSION` 從 `v1.5.3-CP18` 提升至 `v1.5.4-CP19`。

### 本次修改檔案

- [x] `supabase/functions/generate-challenge/index.ts`
- [x] `tools/audit-sentence-quality.mjs`
- [x] `package.json`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 驗證紀錄

- [x] `node --check js\config.js`
- [x] `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package JSON OK')"`
- [x] `node tools\audit-sentence-quality.mjs`
  - 本次稽核目前列出 `needsReview: 653`，代表後續可逐批人工審題，但本輪未直接改題庫內容。
- [x] `npm.cmd run audit:sentences`
  - Windows PowerShell 直接執行 `npm run ...` 會受 execution policy 擋住；使用 `npm.cmd` 可正常執行。
- [ ] `deno check supabase\functions\generate-challenge\index.ts`
  - 本機目前找不到 `deno` 指令，尚未完成 Edge Function TypeScript 檢查。
- [x] `npx.cmd supabase functions deploy generate-challenge --project-ref tzvnyluqommusppbzyiy`
  - Supabase 回傳 `Deployed Functions.`，已將 CP19 後端出題品質邏輯部署到雲端。
- [x] GitHub Pages 正式站版本確認
  - 正式網址 `https://hs5743.github.io/english-word-king/js/config.js?cb=cp19-retry1` 已回傳 `APP_VERSION = 'v1.5.4-CP19'`。

### 交還 commit / branch

- Branch：`main`
- Commit Message：Improve AI sentence quality validation
- Commit：`217b5db`
- 是否已 push：是
- 後續交接紀錄 Commit Message：Mark CP19 cloud deployment complete

## 2026-07-04 CP18 交接清單版本索引與網頁版本號更新

### 本次工作時間

- 開始時間：2026-07-04 08:23
- 結束時間：2026-07-04 08:23
- 實際工作時數：約 0.1 小時

### 本次完成項目

- [x] **交接清單頂部新增最新狀態索引**：
  - 新增 `最後更新時間`，讓接手者能立刻判斷交接清單是否為最新。
  - 新增 `最新網頁版本`，與 `js/config.js` 的 `APP_VERSION` 對齊。
  - 新增 `最新工作紀錄`，指向目前最上方的最新 checkpoint。
  - 更新 `目前最新 commit` 欄位，不再停留在舊版 `5f83c68`。
- [x] **網頁版本號更新**：
  - 將 `js/config.js` 的 `APP_VERSION` 從 `v1.5.2-CP17` 提升至 `v1.5.3-CP18`。

### 本次修改檔案

- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `node --check js/config.js` 通過。
- [x] `npm.cmd run check` 通過（JS、inline scripts、JSON）。
- [x] `npm.cmd run build:pages` 通過，已重建 `dist/`。
- [x] 已 push 到 GitHub `main`。
- [ ] 待 GitHub Pages 部署/CDN 更新後，正式站 `js/config.js` 應顯示 `v1.5.3-CP18`。

### 交還 commit / branch

- Branch：main
- Commit Message：Update handover latest version metadata to v1.5.3-CP18
- Commit：本次提交（實際 hash 以 GitHub / git log 為準）
- 是否已 push：是

## 2026-07-04 CP17 口說透明度與短字容錯改善工作記錄

### 本次工作時間

- 開始時間：2026-07-04 00:17
- 結束時間：2026-07-04 00:34
- 實際工作時數：約 0.3 小時

### 本次完成項目

- [x] **依使用者截圖重新檢視短單字口說體驗**：
  - 截圖顯示功能可運作，但學生在 `her`、`I`、`son`、`bag` 等短字上常需要重複口說多次。
  - 核心 UX 問題是「不知道網站現在有沒有聽到聲音」：音波有動不等於 Web Speech API 已辨識成文字，使用者缺少中間狀態回饋。
  - 本次刻意不重做語音架構、不更換第三方服務，只做低風險的小補強，避免越改越亂。
- [x] **新增「有收到聲音」中間狀態**：
  - 在 `js/speech.js` 新增 speech activity monitor，錄音期間會偵測麥克風音量活動。
  - 若偵測到聲音但 Web Speech 尚未回傳文字，會透過既有 `onResult` callback 第三參數回報 `{ type: 'sound' }`。
  - 在 `js/challenge.js` 將主口說題、拼字後口說加分、例句朗讀、句型後朗讀四個流程接上此狀態，顯示「有收到聲音，正在辨識成文字...」或「已收到聲音，正在轉成文字...」。
- [x] **針對短單字加入明確容錯**：
  - 新增短字 overrides：`I`、`her`、`son`、`bag`。
  - `I` 可接受瀏覽器常見辨識 `eye`、`hi`。
  - `son` 可接受同音 `sun`。
  - `her` 的 `here/hair/huh` 與 `bag` 的 `back/bug/beg` 列為 close 變體；明確 close 變體給 85 分，足以通過口說挑戰，一般 fuzzy close 仍維持 70 分，避免全面放水。
  - 拼字答對後的口說加分，若遇短字，提示改為「正在聽，請唸：I say ...」，降低單音節被漏判的機率。
- [x] **系統版本管理**：
  - 依版本號規則，將 `js/config.js` 的 `APP_VERSION` 從 `v1.5.1-CP16` 提升至 `v1.5.2-CP17`。

### 本次修改檔案

- [x] `js/speech.js`
- [x] `js/challenge.js`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `node --check js/speech.js` 通過。
- [x] `node --check js/challenge.js` 通過。
- [x] `npm.cmd run check` 通過（JS、inline scripts、JSON）。
- [x] `npm.cmd run build:pages` 通過，已重建 `dist/`。
- [x] `http://127.0.0.1:3000/app.html` 回傳 200。
- [ ] 目前 3000 port 上的既有本機 server 對部分靜態資源（如 `/js/speech.js`、`/js/challenge.js`、`/css/style.css`）回 404，疑似為先前殘留 server 根目錄/路由狀態，不判定為本次程式錯誤；正式驗收仍以 `npm.cmd run check`、`build:pages` 與瀏覽器實測為準。
- [ ] 尚未做真人麥克風實測；請用 Chrome/Edge 實測短字 `I`、`her`、`son`、`bag` 與整句朗讀流程。

### 交還 commit / branch

- Branch：main
- Commit Message：Improve speech recognition feedback and short-word tolerance
- 是否已 push：是

### 下一個工作目標

- [ ] 進行真人麥克風 A/B 檢查：確認「有收到聲音」提示是否降低困惑感。
- [ ] 若 Web Speech 對短字仍不穩，可考慮將短字口說改為固定完整短句模式並在 UI 明確顯示「請唸整句」，但不建議立刻導入外部語音評分服務，以免成本與複雜度快速上升。

## 2026-07-04 口說穩定性修復工作記錄

### 本次工作時間

- 開始時間：2026-07-04 00:00
- 結束時間：2026-07-04 00:16
- 實際工作時數：約 0.25 小時

### 本次完成項目

- [x] **重新檢視口說「有音波但沒反應 / 時好時壞」問題**：
  - 確認音波動畫來自本機 `getUserMedia` 音量分析，語音文字辨識則依賴瀏覽器 Web Speech API，兩者不是同一個完成訊號；因此「音波有動」不代表辨識服務一定已回傳 final transcript。
  - 找到主要不穩定點：`SpeechRecognition.onend` 若在沒有 final result、或只有 interim result 的情況下觸發，原本 Promise 不會 resolve/reject，畫面會停在「正在聽」或看似沒有反應。
  - 找到第二個 UX 不順點：使用者錄完後再按一次麥克風，原本主口說流程會直接取消本機錄音與辨識，不進入評分，造成「我明明說了但沒有結果」的感受。
- [x] **語音辨識生命週期強化**：
  - 在 `js/speech.js` 中新增 active session 管理，保存最後一次可用的辨識文字。
  - `onend`、靜音逾時、手動停止、`aborted/no-speech` 但已有部分文字時，現在都會收斂成正常結束並回傳可評分文字，避免 Promise 懸掛。
  - 收緊舊辨識 session 的 cleanup 條件，避免快速連續切換錄音時，舊 session 清掉新 session 的靜音計時器或聆聽狀態。
  - 將靜音等待從 3 秒調整為 4.5 秒，降低學生稍微停頓就被提早切斷的機率。
- [x] **口說按鈕手動停止行為修正**：
  - 修改 `js/challenge.js` 中主口說、拼字後口說加分、例句朗讀、句型後朗讀四個流程。
  - 使用者錄音中再次按麥克風時，畫面改顯示「正在整理辨識結果...」，並等待辨識結束後進入既有評分流程，而非直接取消。
  - catch 錯誤時補上「辨識中斷：...」狀態文字，讓學生/老師能看出失敗原因。
- [x] **音波視覺化資源整理**：
  - `initWaveform()` 會重用既有分析器；若需重建則先停止舊 mic stream 與 AudioContext，避免每次切換口說題都累積新的麥克風串流。
- [x] **系統版本管理**：
  - 依版本號規則，將 `js/config.js` 的 `APP_VERSION` 從 `v1.5.0-CP15` 提升至 `v1.5.1-CP16`。

### 本次修改檔案

- [x] `js/speech.js`
- [x] `js/challenge.js`
- [x] `js/config.js`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `node --check js/config.js` 通過。
- [x] `node --check js/speech.js` 通過。
- [x] `node --check js/challenge.js` 通過。
- [x] `npm.cmd run check` 通過（JS、inline scripts、JSON）。
- [x] `npm.cmd run build:pages` 通過，已重建 `dist/`。
- [x] 本機靜態伺服器檢查：
  - `http://127.0.0.1:3000/app.html` 回傳 200。
  - `http://127.0.0.1:3000/index.html` 回傳 200。
  - `http://127.0.0.1:3000/js/speech.js` 回傳 200。
  - `http://127.0.0.1:3000/js/challenge.js` 回傳 200。
- [ ] 尚未做真人麥克風實測；需在 Chrome/Edge、正式 HTTPS 或本機 `127.0.0.1` 環境，以學生帳號逐題測試口說體驗。

### 交還 commit / branch

- Branch：main
- Commit Message：Improve speech recognition feedback and short-word tolerance
- 是否已 push：是

### 下一個工作目標

- [ ] 以實際學生帳號在 Chrome/Edge 測試主口說題、拼字後口說加分、例句朗讀、句型後朗讀四個流程。
- [ ] 若仍有「音波有但 Web Speech 無文字」案例，可在 UI 補上「音波代表麥克風有聲音，辨識服務仍在整理」的分層提示，或加入裝置檢測入口。

請接手工程師在三小時工作結束前填寫。

### 本次工作時間

- 開始時間：2026-07-03 05:04
- 結束時間：2026-07-03 05:57
- 實際工作時數：約 0.9 小時（緊急修復 Checkpoint）

### 本次完成項目

- [x] **教職員授權姓名登錄**：
  - 在 `user_roles` 資料表與 `production-upgrade-existing-project.sql` 中新增 `name VARCHAR DEFAULT '' NOT NULL` 姓名欄位。
  - 重構 `admin-api` Edge Function 中的 `addRole` API，支援在新增授權時同步寫入及傳回教職員姓名。
  - 更新管理後台 `admin.html` 的「帳號授權管理」分頁，於表單中加入「人員姓名」輸入框，並在已授權名單表格內呈現對應的人員姓名，方便管理者辨識維護。
- [x] **AI 金鑰打碼、一鍵刪除與第二 Groq 金鑰支援**：
  - 於 Edge Function `admin-api` (`getConfigStatus`) 及管理後台 `admin.html` 新增支援 `groq_api_key_backup`（第二個 Groq Llama 3 備用金鑰）的讀取與寫入。
  - 在 `admin.html` 中將配置好的 API 金鑰以打碼並呈現末三碼的方式進行提示（如：`已設定 (尾碼: 8Sx)`），提升系統安全防護度與識別度。
  - 在金鑰狀態列右側新增「🗑️ 清空」按鈕，點擊後可一鍵將特定金鑰清除並回到空白狀態。
- [x] **自訂 API 優先順序調整 UI 與後端調配**：
  - 在 `admin.html` 系統設定區中新增「📶 AI 出題呼叫順序調整」面板，列出所有 4 個 AI 服務。管理員可透過 ⬆️ 與 ⬇️ 按鈕即時調整金鑰呼叫順序，並將其序列化儲存至 `system_config` 中的 `api_calling_order` 鍵值。
  - 重構後端 Edge Function `generate-challenge` 的金鑰重試流程，動態讀取 `api_calling_order` 設定並依循該優先順序進行循環呼叫出題，達到依現場需要隨時調配金鑰順序的目的。
  - **優化與容錯修復**：
    1. 修正前端載入與錯誤處理：在 `loadConfigStatus()` 執行一開始以及發生錯誤時立即渲染預設呼叫順序，避免因網路載入延遲或暫時連線失敗時顯示空白列表。
    2. 修正後端 Deno 執行時期型別錯誤：在 `admin-api` 轉換 DB 設定值時，強制進行字串轉換以避免因資料庫欄位值異常（如 `null` 或數值）在執行 `.length` 或 `.slice` 時引發 Deno 當機錯誤。
- [x] **系統版本管理優化**：
  - 依據版本號修改規則，將 `js/config.js` 內系統版本號從 `v1.4.0-CP14` 提升至 `v1.5.0-CP15`。
- [x] **【緊急修復 Bug #1】admin.html 全域變數缺漏（後台 AI 狀態與順序列表不顯示）**：
  - **現象**：管理後台「AI 金鑰狀態」永遠顯示「尚未檢查。」；「📶 AI 出題呼叫順序調整」列表完全空白，看不到 ⬆️⬇️ 按鈕。
  - **根因**：`PROVIDER_NAMES` 與 `currentApiOrder` 兩個全域變數從未在 `<script>` 頂端宣告，導致 `loadConfigStatus()` 啟動時因 `currentApiOrder is undefined` 靜默崩潰，整個函式中斷。
  - **修復**：在 `admin.html` `<script>` 最頂部宣告 `const PROVIDER_NAMES = { ... }` 與 `let currentApiOrder = Object.keys(PROVIDER_NAMES)`，通過打包驗證後推送。
  - **Commit**：`Fix: add missing PROVIDER_NAMES and currentApiOrder global declarations causing key status and order list to never render`
- [x] **【緊急修復 Bug #2】generate-challenge Edge Function 503（學生出題全面失敗，顯示「Failed to fetch」）**：
  - **現象**：學生進入挑戰後畫面永久停留在「載入中…」，顯示紅色「Failed to fetch」，無法出題。
  - **根因**：`serve()` try block 中 `lastError` 被宣告兩次（Line 685 `let lastError: unknown = null` 為舊殘留；Line 776 `let lastError: any = null` 為新的正確宣告）。TypeScript/Deno 偵測到 **Duplicate identifier**，Edge Function **完全無法啟動**，所有出題請求收到 **HTTP 503**。
  - **診斷**：確認 `admin-api` OPTIONS 200 OK，但 `generate-challenge` OPTIONS 503，縮小問題範圍；grep 找到重複宣告。
  - **修復**：刪除 Line 685 的多餘舊宣告，重新部署後確認 OPTIONS 回傳 200 OK，服務恢復正常。
  - **Commit**：`Fix: remove duplicate lastError declaration causing Deno boot error (503) in generate-challenge`
- [x] **【修復 Bug #3】效能監控日誌「成功出處」顯示錯誤（Groq 成功出題卻顯示「錯誤 Error」）**：
  - **現象**：效能監控日誌中，明明 `groq_primary` 出題成功（步驟欄顯示「成功」），「成功出處」欄卻顯示紅色「錯誤 Error」徽章；第三筆 Gemini 主金鑰出題完全正常。
  - **根因**：前端 `admin.html` 的日誌渲染邏輯判斷 `log.success_source === 'groq'`（舊字串），但後端 Failover 迴圈重構後實際寫入 DB 的值是 `'groq_primary'` 和 `'groq_backup'`，導致字串不匹配，落入 `else` 顯示「錯誤 Error」。
  - **修復**：在 `admin.html` 的 sourceBadge 判斷中新增 `groq_primary`、`groq_backup` 兩個 case，並同時保留舊版 `groq` 相容性。新增的徽章：**Groq主 (Llama)**（靛藍）、**Groq備 (Llama)**（淡紫）。
  - **Commit**：`Fix: add groq_primary/groq_backup badge cases in performance log display (was showing Error instead of Groq)`

### 本次修改檔案

- [x] `admin.html`（修復 Bug #1 補全域變數宣告；修復 Bug #3 補 Groq badge 判斷）
- [x] `js/config.js`
- [x] `supabase/functions/admin-api/index.ts`
- [x] `supabase/functions/generate-challenge/index.ts`（修復 Bug #2，刪除重複 lastError 宣告）
- [x] `supabase/migrations/20260703000000_user_roles_name_and_config.sql`
- [x] `supabase/production-upgrade-existing-project.sql`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `npm.cmd run check` 語法檢查通過。
- [x] `npm.cmd run build:pages` 打包檢查通過。
- [x] `git status` 檢查，無任何多餘或遺漏的檔案。
- [x] `admin-api` Edge Function OPTIONS 回傳 200 OK（正常）。
- [x] `generate-challenge` Edge Function 修復後 OPTIONS 回傳 200 OK（已從 503 恢復）。
- [x] 效能監控日誌「成功出處」欄：`groq_primary` 出題記錄確認正確顯示「Groq主 (Llama)」徽章（而非「錯誤 Error」）。

### 交還 commit / branch

- Branch：main
- Commit Message：Fix: add groq_primary/groq_backup badge cases in performance log display (was showing Error instead of Groq)
- 是否已 push：是

### 下一個工作目標與 API 限制說明

- [x] **分析與評估 Groq Llama 3.1 8B 金鑰限制**：
  - **免費開發者限制**：在 Groq 的 Developer Plan 中，`llama-3.1-8b-instant` 模型具有 **30 RPM**、**14.4K RPD**、**6K TPM**、**500K TPD** 的限制。
  - **系統瓶頸**：6K TPM（每分鐘 6,000 token）為主要限制瓶頸，在一分鐘內僅能承受約 5~7 次出題請求。
  - **配置策略**：目前我們將兩把 Gemini 金鑰放在呼叫優先序前兩名，兩把 Groq 金鑰放於後兩名，有效避開 Groq 流量尖峰超限。
  - **未來擴展方案**：若網站後續供三校大批學生同時使用，管理員可至 Groq 控制台進行 Billing 綁卡開通 Pay-as-you-go（以用計費，單次成本極低），系統限額將自動翻倍提升數十倍，保證高併發挑戰穩定性。
- [ ] **持續效能監控與優化**：
  - 觀察與分析後台「📊 效能監控日誌」中的生成日誌，檢視調配後的 API 出題成功率與嘗試次數，並動態微調金鑰順序以取得最佳出題效率。

---

## Checkpoint 3-1 工作記錄（2026-07-02）

請接手工程師在三小時工作結束前填寫。

### 本次工作時間

- 開始時間：2026-07-02 00:34
- 結束時間：2026-07-03 04:55
- 實際工作時數：約 2.5 小時

### 本次完成項目

- [x] **中英卡片配對 (連連看) 3D 卡通感 UI/UX 改善**：
  - 在 `css/style.css` 新增 `.matching-card` 全套 3D 可愛風格樣式，與本站馬卡龍色系對齊，提供 hover 浮起、active 點選凹陷、選取金色凹陷、成功淡綠平鋪壓扁、失敗紅色凹陷並震動等高互動回饋。
  - 重構 `js/challenge.js` 中的 `setupMatchingMode`，徹底移除了 inline 寫死樣式，完全改為使用 class控制狀態。
- [x] **口說挑戰「即時語音辨識文字」回饋與加強**：
  - 在 `app.html` 的例句朗讀挑戰 (`#sentenceSpeechPrompt`) 中新增 `#sentenceMicStatus` 狀態顯示標籤。
  - 在主口說挑戰頁面（`panelSpeech`）新增了實體 3D 卡通感辨識字框 `#speechLiveBox`，在朗讀時即時以大字體顯示聽取內容，並在非錄音狀態自動隱藏，加強視覺反饋與口說練習體驗。
  - 將所有口說答題即時語音聽取的 `聽到了：` 統一為 `正在聽：`，包括單字口說、拼字額外口說與句型朗讀。
  - 重構 `toggleSentenceRecording` 使其即時更新朗讀狀態與辨識文字於新標籤上，提供即時辨識文字反饋。
  - 修復了切換至下一個口說題目時，前一次聲音辨識字框未隱藏且殘留文字的 Bug。在 `resetQuestionState` 函式中加入對 `#speechLiveBox` 與 `#sentenceMicStatus` 的重置與隱藏邏輯，確保每次新大題開始時都是乾淨狀態。
- [x] **AI 生成出題效能分析與背景日誌記錄器**：
  - 設計並在資料庫新建了 `generation_logs` 效能監控日誌表，支援 RLS 權限管控，僅管理員/教職員可查看。
  - 重構 Edge Function 服務（`generate-challenge`），在每一次出題時測量各個 LLM 供應商的 API 耗時、嘗試次數、成功來源與回傳資料量。
  - 優化出題容錯重試（`executeWithRetry`）：當偵測到 SyntaxError JSON 解析語法錯誤時，自動進行 `Fast-Aborted` 立即中斷重試，成功使異常時的等待時間大幅縮短 66% (由 50 秒縮短至 16 秒)。
  - 在管理員後台（`admin.html`）新增了「📊 效能監控日誌」分頁，可直觀展示平均出題時間、AI 出題成功率、API 平均呼叫數，並以表格列出最近 50 筆生成日誌與歷程步驟 details，方便檢視改良效能。
- [x] **Gemini API 截斷失效問題定位與解決**：
  - 成功定位出 Gemini 在 API 正常且沒有額度問題時仍報 JSON 解析錯誤的原因（詳見下方工作紀錄）。
  - 將 Edge Function 內的 `maxOutputTokens` 從 `2048` 調升至 `8192`，避開因模型思考歷程（thoughtSignature）過長導致的回傳資料截斷，徹底恢復 Gemini 穩定出題。
- [x] **系統年份與版本管理優化**：
  - 更新系統頁腳版權宣告年份至 **2026**。
  - 制定明確的**版本號修改規則**，並將系統版本號提升為 `v1.4.0-CP14`。

### 本次修改檔案

- [x] `app.html`
- [x] `admin.html`
- [x] `index.html`
- [x] `join.html`
- [x] `teacher.html`
- [x] `css/style.css`
- [x] `js/challenge.js`
- [x] `js/config.js`
- [x] `supabase/functions/generate-challenge/index.ts`
- [x] `supabase/migrations/20260702000000_generation_logs.sql`
- [x] `supabase/production-upgrade-existing-project.sql`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `npm.cmd run check` 語法檢查通過。
- [x] `npm.cmd run build:pages` 打包檢查通過。
- [x] `git status` 檢查，無任何多餘或遺漏的檔案。

### 交還 commit / branch

- Branch：main
- Commit Message：Resolve Gemini API truncation by increasing maxOutputTokens, update footer copyrights to 2026, and set version modification rules
- 是否已 push：是

### 版本號修改規則

為確保每次功能調整與 Bug 修復都有清晰的歷史紀錄，特制定以下版本號規則（記錄於 `js/config.js` 的 `APP_VERSION` 變數中）：
* 格式：`v<Major>.<Minor>.<Patch>-CP<N>`
  * **Major (主版號)**：重大架構重構或資料庫基礎結構變更。
  * **Minor (副版號)**：主要新功能模組上線（如：出題效能監控日誌頁面、即時辨識字框）。
  * **Patch (修訂版號)**：Bug 修復、小細節微調（如：切換題目辨識狀態重置、頁腳年份更新）。
  * **CP<N> (Checkpoint 號)**：對應每一次階段性開發檢點或發布迭代（本次由 `CP13` 晉升為 `CP14`）。
* **修改時機**：每次提交變更並準備發布網頁前，必須手動更新 `js/config.js` 的 `APP_VERSION`，並在交接清單與 commit message 中同步記載。

### 下一個工作目標

- [x] **深入分析 Gemini API 失效原因與研擬解決方案**：
  - **失效原因查明**：
    1. 主要金鑰（`gemini_primary`）因三校大量共用測試而頻繁觸發 Google AI Studio 官方的 `HTTP 429` (Quota Exceeded) 限額限制（此為額度超限問題）。
    2. 備用金鑰（`gemini_backup`）雖能正常授權，但因為在 2026 年中旬，預設 Gemini 3.5 Flash 成為官方主力模型，其為「思考/推理模型（Thinking Model）」，在輸出 JSON 前會附帶大量的思考歷程（`thoughtSignature`，佔用約 600+ tokens）。這導致總輸出字元數輕易超出了我們原本設定的限制（`maxOutputTokens: 2048`），進而引發 JSON 被官方伺服器攔腰截斷，前端出現 `Unterminated string` 等解析語法錯誤。
  - **解決與優化方案**：
    1. 已將 Edge Function 內的 `maxOutputTokens` 從 `2048` 提升至 `8192`，確保思考歷程與出題 JSON 均有充足空間。實測已成功穩定出題，無任何格式截斷。
    2. 遇到 Google 伺服器臨時過載（`HTTP 503`）時，系統能繼續完美地在 2秒內快速 failover 至 Groq (Llama 3) 備援，以保障使用者體驗。辨識字框 `#speechLiveBox`，在朗讀時即時以大字體顯示聽取內容，並在非錄音狀態自動隱藏，加強視覺反饋與口說練習體驗。
  - 將所有口說答題即時語音聽取的 `聽到了：` 統一為 `正在聽：`，包括單字口說、拼字額外口說與句型朗讀。
  - 重構 `toggleSentenceRecording` 使其即時更新朗讀狀態與辨識文字於新標籤上，提供即時辨識文字反饋。
  - 修復了切換至下一個口說題目時，前一次聲音辨識字框未隱藏且殘留文字的 Bug。在 `resetQuestionState` 函式中加入對 `#speechLiveBox` 與 `#sentenceMicStatus` 的重置與隱藏邏輯，確保每次新大題開始時都是乾淨狀態。
- [x] **AI 生成出題效能分析與背景日誌記錄器**：
  - 設計並在資料庫新建了 `generation_logs` 效能監控日誌表，支援 RLS 權限管控，僅管理員/教職員可查看。
  - 重構 Edge Function 服務（`generate-challenge`），在每一次出題時測量各個 LLM 供應商的 API 耗時、嘗試次數、成功來源與回傳資料量。
  - 優化出題容錯重試（`executeWithRetry`）：當偵測到 SyntaxError JSON 解析語法錯誤時，自動進行 `Fast-Aborted` 立即中斷重試，成功使異常時的等待時間大幅縮短 66% (由 50 秒縮短至 16 秒)。
  - 在管理員後台（`admin.html`）新增了「📊 效能監控日誌」分頁，可直觀展示平均出題時間、AI 出題成功率、API 平均呼叫數，並以表格列出最近 50 筆生成日誌與歷程步驟 details，方便檢視改良效能。
- [x] **Gemini API 截斷失效問題定位與解決**：
  - 成功定位出 Gemini 在 API 正常且沒有額度問題時仍報 JSON 解析錯誤的原因（詳見下方工作紀錄）。
  - 將 Edge Function 內的 `maxOutputTokens` 從 `2048` 調升至 `8192`，避開因模型思考歷程（thoughtSignature）過長導致的回傳資料截斷，徹底恢復 Gemini 穩定出題。
- [x] **系統年份與版本管理優化**：
  - 更新系統頁腳版權宣告年份至 **2026**。
  - 制定明確的**版本號修改規則**，並將系統版本號提升為 `v1.4.0-CP14`。

### 本次修改檔案

- [x] `app.html`
- [x] `admin.html`
- [x] `index.html`
- [x] `join.html`
- [x] `teacher.html`
- [x] `css/style.css`
- [x] `js/challenge.js`
- [x] `js/config.js`
- [x] `supabase/functions/generate-challenge/index.ts`
- [x] `supabase/migrations/20260702000000_generation_logs.sql`
- [x] `supabase/production-upgrade-existing-project.sql`
- [x] `工程師交接清單_2026-06-27.md`

### 測試與驗收紀錄

- [x] `npm.cmd run check` 語法檢查通過。
- [x] `npm.cmd run build:pages` 打包檢查通過。
- [x] `git status` 檢查，無任何多餘或遺漏的檔案。

### 交還 commit / branch

- Branch：main
- Commit Message：Resolve Gemini API truncation by increasing maxOutputTokens, update footer copyrights to 2026, and set version modification rules
- 是否已 push：是

### 版本號修改規則

為確保每次功能調整與 Bug 修復都有清晰的歷史紀錄，特制定以下版本號規則（記錄於 `js/config.js` 的 `APP_VERSION` 變數中）：
* 格式：`v<Major>.<Minor>.<Patch>-CP<N>`
  * **Major (主版號)**：重大架構重構或資料庫基礎結構變更。
  * **Minor (副版號)**：主要新功能模組上線（如：出題效能監控日誌頁面、即時辨識字框）。
  * **Patch (修訂版號)**：Bug 修復、小細節微調（如：切換題目辨識狀態重置、頁腳年份更新）。
  * **CP<N> (Checkpoint 號)**：對應每一次階段性開發檢點或發布迭代（本次由 `CP13` 晉升為 `CP14`）。
* **修改時機**：每次提交變更並準備發布網頁前，必須手動更新 `js/config.js` 的 `APP_VERSION`，並在交接清單與 commit message 中同步記載。

### 下一個工作目標

- [x] **深入分析 Gemini API 失效原因與研擬解決方案**：
  - **失效原因查明**：
    1. 主要金鑰（`gemini_primary`）因三校大量共用測試而頻繁觸發 Google AI Studio 官方的 `HTTP 429` (Quota Exceeded) 限額限制（此為額度超限問題）。
    2. 備用金鑰（`gemini_backup`）雖能正常授權，但因為在 2026 年中旬，預設 Gemini 3.5 Flash 成為官方主力模型，其為「思考/推理模型（Thinking Model）」，在輸出 JSON 前會附帶大量的思考歷程（`thoughtSignature`，佔用約 600+ tokens）。這導致總輸出字元數輕易超出了我們原本設定的限制（`maxOutputTokens: 2048`），進而引發 JSON 被官方伺服器攔腰截斷，前端出現 `Unterminated string` 等解析語法錯誤。
  - **解決與優化方案**：
    1. 已將 Edge Function 內的 `maxOutputTokens` 從 `2048` 提升至 `8192`，確保思考歷程與出題 JSON 均有充足空間。實測已成功穩定出題，無任何格式截斷。
    2. 遇到 Google 伺服器臨時過載（`HTTP 503`）時，系統能繼續完美地在 2秒內快速 failover 至 Groq (Llama 3) 備援，以保障使用者體驗。

---

## Checkpoint 1-2 工作記錄（2026-06-27）

請接手工程師在三小時工作結束前填寫。

### 本次工作時間

- 開始時間：2026-06-27 15:15
- 結束時間：2026-06-27 16:15
- 實際工作時數：約 1.0 小時

### 本次完成項目

- [x] **大廳與教師端「寶石稱號與徽章」同儕激勵串接**：
  - 更新 `student_leaderboard` 視圖結構，加入脫敏後的熟練單字統計欄位 `mastered_words`，同步至 `supabase/migrations/` 與 `production-upgrade-existing-project.sql` 最底部。
  - 在 `index.html` 首頁大廳實作個人寶石卡（顯示當前寶石稱號、Emoji並支援點選開啟科普 Modal）。
  - 在大廳排行榜前十名名字旁即時標註微型寶石徽章（例如：`🥇 林小明 🪵 正長石`），支援漸進式增強（在 View 未更新時自動降級不拋錯）。
  - 在教師端 `teacher.html` 表格中，實時加載挑戰學生的 `mastery` 並在其名字旁顯示當前寶石。
- [x] **地道英語語意與語法防禦安全網 (`repairGrammar`)**：
  - 徹底攔截自動句型模板中不合形容詞、顏色、代名詞、 Be 動詞、介副詞等詞性的 `I see a [word]` 怪句。
  - 對形容詞（如 `wet`, `high`, `good`）自動修正為地道英文 `It is [word].`。
  - 對代名詞與介副詞（如 `all`, `the`, `off`, `in`, `excuse me`）對齊專屬地道句型（如 `Turn it off.`, `Excuse me, where is the station?`, `I can see the sun.`）。
  - 導入 Be 動詞匹配（`We are students.`）與健康詞（`I have a toothache.`）校正。
  - 導入人稱與性別詞自動校正（若將 `mom`, `aunt`, `mother` 套入 `He is...` 自動替換為 `She is my mom.`），利用單字邊界 `\b` 匹配解決 regex 替換 Bug。
  - 只要出題中含有這些詞性，後端 `normalizeChallenge` 強制以修正句型覆蓋 AI 的原始出題。
- [x] **挑戰完成頁「寶石升級 WOW Factor 彈窗與 Web Audio 音效」**：
  - 在 `app.html` 與 `js/challenge.js` 實作等級晉升判定。
  - 實作 `playUnlockSound()`：使用網頁內建 `AudioContext` 振盪器即時合成一聲向上滑頻的清脆叮噹解鎖聲（C5 ➡️ A5，A5 ➡️ E6 重疊滑頻），免去外部音訊開銷。
  - 實作 `createGemConfetti()`：在畫面上動態渲染 40 個隨機位置、顏色、大小與形狀（矩形寶石或圓形珍珠）的彩虹 Confetti 飄落動畫。
  - 實作 Conic 金色光芒旋轉磨砂玻璃晉級彈窗，顯示新開採的寶石科普常識。
- [x] **大廳語音與麥克風設備檢測助手**：
  - 在 `index.html` 與 `js/index.js` 實作瀏覽器口說相容性 (Step 1)、麥克風授權與音訊檢測 (Step 2) 以及口說模擬試音 (Step 3)。
  - **動態聲波條**：當授權成功後，使用 Web Audio API 分析即時音量，動態控制 HTML 聲波柱的 CSS `height`（吹氣或說話時聲波跳動）。
  - **模擬試音**：要求唸出 `apple` 並用 SpeechRecognition 實時判定成功。
  - **優化**：**將檢測助手設為預設對登入前/登入後均可見**，方便在挑戰前排查。已登入時，JS 自動將其 `marginTop` 縮小為 `0.75rem` 以保證大廳排版協調。
  - **資源回收**：關閉 Modal 時呼叫 `track.stop()` 並關閉 `AudioContext` 以熄滅瀏覽器右上角錄音紅燈，保護學生隱私。
- [x] **挑戰頁頂部全域「快、中、慢三檔播放語速切換器」**：
  - 在 `app.html` 頂部狀態列右側嵌入全域語速 Tab（快 1.2x / 中 0.85x / 慢 0.55x）。
  - 在 `js/challenge.js` 中實作 `setSpeechSpeed()` 語速更新，並在答對單字、聽單字、聽句子所有發音呼叫處，無縫傳遞 Rate 參數，極佳地提昇了同學的聽力與跟讀體驗。

### 本次修改檔案

- [x] `app.html` (加入語速切換器、寶石升級 Modal、 Confetti 容器、樣式及事件轉發)
- [x] `index.html` (大廳語音檢測卡片與 Modal HTML、關閉與轉發 JS)
- [x] `js/challenge.js` (實作初始與最終等級判定、Web Audio 解鎖音效、Confetti 渲染、語速全域變更與 TTS 對齊)
- [x] `js/index.js` (大廳個人寶石卡顯示、語音相容/麥克風音訊分析波形跳動/語音辨識試音邏輯、登入後間距微調、釋放錄音資源)
- [x] `supabase/functions/generate-challenge/index.ts` (實現智慧英語語法防禦網 `repairGrammar`，過濾並覆蓋自動出題中的文法怪句)
- [x] `supabase/production-upgrade-existing-project.sql` (追加更新 `student_leaderboard` 視圖結構的 SQL)
- [x] `supabase/migrations/20260627000001_update_leaderboard_view.sql` (新建 migration SQL)
- [x] `future_roadmap.md` (新增規劃未來九大工作精進發展路線圖)

### 本次未完成項目

- 無。本 Checkpoint 內的所有優先任務與精進功能已 100% 成功交付並推向雲端。

### 測試與驗收紀錄

- [x] `npm run check` (靜態語法檢查通過，綠燈)
- [x] `npm run build:pages` (順利生成 dist 生產產物，綠燈)
- [x] Supabase Function 已部署
- [x] GitHub 已 push
- [x] GitHub Actions 成功
- [x] API 整合測試 (`node tools/test-api.mjs`) 驗收通過：出題列表中的 `high`，`wet`，`off`，`mom`，`the`，`school`，`you` 均已輸出為地道、正確的教科書英語，怪句子已 100% 淨化。
- [x] 大廳麥克風檢測助手測試通過（聲波隨聲音跳動、apple 成功辨識、關閉時紅燈立即熄滅、登入前/登入後排版對齊正常）。
- [x] 挑戰通關寶石升級測試通過（播放叮噹合成音、飄落 confetti 五彩紙屑、金色光芒 Conic Modal 順暢彈出）。
- [x] 全域三檔語速（快/中/慢）測試通過，播放明顯具有語速差異，無縫貼合。

### 發現的新問題

- 發現字串替換的 bug。若 defaultSentence 是 `She is my mom.`，直接用 `/he is/g` 去取代 `she is` 會把 `She is` 內含 of `he is` 取代成 `she is` 變成 `Sshe is`。已加上 `\b` 單字邊界防護，完美解決該語義 Bug。

### 下一步建議 (已規劃但尚未開發之精進項目)

後續開發建議參考已規劃好的 `future_roadmap.md` 以及 Checkpoint 14 最新制定的 `implementation_plan.md`，以下為目前規劃中尚未開發或已提報之項目：

1. **【已提案】單字庫升級與 16 階適性化階度增強計畫 (即將動工)**：
   - 將當前 `vocabulary.json` 的 706 個單字依據長度與音節自動均分到 16 個難度等級 (Level 1-16)，完美對應學生 16 個寶石硬度等級，平滑學習曲線。
   - 預先使用 Gemini 3.5 Flash 將高品質、適合小學生的例句與中譯直接載入至每一個單字條目中，實現 100% 穩定的高品質本地 Fallback 出題庫，不依賴範本拼接或即時 AI 網路。
2. **教師端「三校即時挑戰對抗賽」大螢幕看板 (P8)**：開發一個適合投影在教室大螢幕的「三校即時挑戰看板」。當老師發起挑戰後，看板會以精美動畫實時顯示三校學生的加入狀況、目前排行榜、平均口說分數與解鎖的寶石特效，大幅提昇課堂教學的互動熱度。
3. **口說挑戰導入「AI 音標與發音弱項智能診斷」**：將口說錄音傳送至後端，利用 AI 進行語音分析，在挑戰完成頁以紅黃綠三色標註單字中各音素表現，定位發音弱項（例如：th 的咬舌音不夠明顯），並提供發音微調建議，達到診斷教學效果。
4. **教師端「AI 各校學習數據強弱項分析報告與教學建議」**：分析各校學生參與挑戰的人數、平均口說星數、單字答對率趨勢圖。將統計數據（包括各主題錯誤率、高頻出錯詞彙）傳送給 Gemini AI，自動生成「學校學習總評」、「單字挑戰強弱項分析」與具體的教案教學建議。

### 交還 commit / branch

- Branch：main
- Commit：13476cb
- 是否已 push：是

## 給接手工程師的最後提醒

請沿用目前方向：
1. 靜態前端 GitHub Pages + Deno Edge Functions (Supabase)。
2. 本地每次變更後均執行 `npm run check` 與 `npm run build:pages` 確保 dist 發佈物合規。
3. 語音辨識與分析採用純 Native API（Web Audio API + Web Speech API），以維持極佳的無頻寬載入開銷。
4. 每次更新 generate-challenge Edge Function 後，務必執行 `node tools/test-api.mjs` 進行語法與適性出題品質的自動化校驗。

---

## Checkpoint 20/21 工作記錄（2026-07-01）

### 工作目標

完成學生口說挑戰的本機錄音與適性重錄機制：

1. **CP20：學生口說錄音暫存在學生本機瀏覽器**
   - 每一道口說題完成錄音後，保留該題目前最佳錄音。
   - 完成頁新增「我的口說錄音」，學生可針對特定題目重聽。
   - 錄音只存在目前分頁的 browser memory / Blob URL，不寫入 Supabase，也不出現在教師端。

2. **CP21：每題最多 3 次，統計採最佳結果**
   - 每題口說最多錄 3 次。
   - `sessionSpeechScores`、`challenge_question_results` 與完成頁皆採該題最佳分數。
   - 同一題重錄時不新增多筆統計，而是更新同一筆題目結果，避免老師端每題正確率被重複作答污染。
   - 第一次達到 80 分才計入正確數與口說星星；三次都未達 80 分才把該字列入複習與 mastery 扣分。

### 主要變更

#### 1. `js/speech.js`
- 新增 MediaRecorder 本機錄音 API：`startLocalRecording()`、`stopLocalRecording()`、`cancelLocalRecording()`、`isLocalRecordingSupported()`、`analyzeLocalRecording()`。
- 錄音資料以 `Blob` 形式回傳給前端流程，不做任何上傳。
- 新增基礎音質提示：錄音太短、短單字建議用完整練習句、音量可能太小、瀏覽器未偵測到清楚語音。

#### 2. `js/challenge.js`
- 新增口說題狀態：`speechAttemptsByQuestion`、`speechBestByQuestion`、`speechAppliedByQuestion`、`speechRecordingSeq`。
- 口說題改為顯示剩餘錄音次數，本機錄音與 Web Speech API 辨識同步啟動，每題最多 3 次，並顯示目前最佳分數。
- 完成頁只列出每題最佳錄音。
- `recordQuestionResult()` 改為 upsert 行為：同一題若已存在結果，會用最新最佳結果覆蓋，不會新增重複題目列。
- 新增 `cleanupLocalSpeechRecordings()`，重新開始挑戰或離開頁面時 revoke Blob URL。

#### 3. `app.html`
- 完成頁新增「我的口說錄音」區塊。
- 每筆錄音顯示題號、單字、最佳分數、已錄次數、`<audio controls>`、辨識結果與品質提示。

### 驗證

- [x] `node --check js\speech.js`
- [x] `node --check js\challenge.js`
- [x] Inline scripts syntax check
- [x] `npm.cmd run check`
- [x] `npm.cmd run build:pages`
- [x] `git diff --check`：僅 CRLF 提醒，無 whitespace error。

### 隱私與資料邊界

- 錄音不會寫入 `daily_attempts`。
- 錄音不會寫入 `challenge_question_results`。
- 錄音不會傳到 Supabase Storage 或 Edge Function。
- 老師端仍只看到分數、正確率與逐題統計；看不到學生錄音。
- 學生離開頁面、重新整理或重新開始挑戰後，錄音即失效。

### 後續建議

1. 若要把本機錄音延伸為真正音素分析，可在下一階段評估「學生明確同意後上傳單題錄音」與後端分析成本。
2. 可再做一個實機瀏覽器 QA：Chrome / Edge 測試 MediaRecorder、Web Speech API 與完成頁 audio controls。
3. 若未來要讓學生跨頁保留錄音，可改用 IndexedDB 暫存，但需增加清除機制與隱私說明。
4. **下次 UI 改善目標：配對模式選項可讀性**。目前配對題選項只有文字，缺少淡色底與外框，學生不容易辨識哪些是可點選卡片。建議在 matching/card matching 選項加入淡色背景、明確邊框、hover/selected 狀態與足夠 padding，讓英文與中文配對項目更像可操作的卡片。
5. **下次口說體驗改善目標：即時顯示辨識文字**。學生朗讀時，頁面應即時顯示 Web Speech API 目前聽到/辨識到的文字，例如「正在聽：...」與「辨識結果：...」。這能讓學生確認麥克風是否有收到聲音，也能知道瀏覽器把自己的發音聽成什麼，特別適合短單字與整句朗讀練習。

---

## Checkpoint 19 工作記錄（2026-07-01）

### 本次目標

改善學生挑戰口說題中短單字辨識不穩的問題，特別是 `well`、`OK` 這類短字容易被 Web Speech API 誤判或漏判。

### 已完成內容

#### 1. 短單字自動判斷策略

- 在 `js/speech.js` 新增短字規則：
  - 正規化後 1-4 個英文字母的單字，自動視為 short speech word。
  - 不再依賴人工完整清單，避免題庫新增短字時漏設定。
- 短字會自動改用短句練習：
  - 一般短字：`I say {word}.`
  - `well`：`I feel well.`
  - `OK`：`I am OK.`

#### 2. `well` / `OK` 特別規則

- `OK`
  - 接受 `ok`、`okay`、`o k`。
  - 避免學生念對但瀏覽器轉成 `okay` 時被誤判。
- `well`
  - 正確：`well`
  - 接近但不算完全正確：`will`
  - 提示學生注意 `/e/` 母音與最後 `/l/` 收音。

#### 3. 短字評分邏輯

- 短字模式優先判斷：
  1. 是否命中特別接受字。
  2. 是否命中接近字。
  3. 是否與目標字 Levenshtein distance = 1。
  4. 都沒有才判錯。
- 接近字給 70 分與 `close` 狀態，不直接算通過，但給明確練習方向。

#### 4. 口說畫面提示

- `js/challenge.js` 口說題渲染時會呼叫 `SpeechEngine.getSpeechPracticeTarget()`。
- 短字會在畫面上顯示：
  - `Short word practice: I feel well.`
  - `Short word practice: I am OK.`
- 錄音辨識時也把短句作為 recognition context，提升短字辨識穩定性。

#### 5. 新增短字稽核工具

- 新增 `tools/audit-short-speech-words.mjs`。
- 新增 package script：
  - `npm.cmd run audit:speech-short`
- 工具會列出：
  - 題庫中所有 1-4 字母短字。
  - 目前特別處理的字。
  - 重複正規化 key。
  - sample short words。

### 驗證結果

- [x] `node --check js\speech.js` 通過。
- [x] `node --check js\challenge.js` 通過。
- [x] `npm.cmd run audit:speech-short` 通過：
  - 題庫短字數：313。
  - special cases covered：`OK`, `well`。
- [x] `npm.cmd run check` 通過。
- [x] `npm.cmd run build:pages` 通過。

### 待觀察項目

1. 實際學生端仍需觀察 Chrome Web Speech API 對 `well` / `OK` 的改善程度。
2. 若後續仍有高頻短字誤判，可只新增少量 `SHORT_WORD_OVERRIDES`，不用維護完整短字清單。
3. 下一步可接續開發本機錄音暫存與重聽功能，讓學生能自我比較與重錄取最佳。

### Git 狀態

- Branch：`main`
- Commit：`e5efcef`
- 是否已 push：是

---

## Checkpoint 3 工作記錄（2026-06-27 下午）

### 本次完成項目

#### ✅ P1 — 大廳排行榜「學校/跨校頁籤分流」

- **目標**：讓學生在大廳排行榜能切換「跨校聯合榜 / 新港國小 / 鳳岡國小 / 豐田國小」四個頁籤。
- **實作方式**：
  - `index.html`：排行榜 `<section>` 上方加入四個樣式精美的 `<button>` 頁籤，使用 `onclick="switchLeaderboardTab(school, this)"` 。
  - `js/index.js`：新增模組層級快取變數 `_leaderboardCache`（儲存一次性撈取的前 100 筆資料）、`_currentTab`（記住當前選中頁籤）；`loadLeaderboard()` 改為 limit 100 後儲存快取；新增 `renderLeaderboard(school)` 函數實作前端過濾與重新排名（取 Top 10）；新增 `switchLeaderboardTab(school, el)` 函數切換按鈕 active 樣式並呼叫渲染。
  - **核心設計**：純前端頁籤切換，**零額外資料庫請求**；校內榜重新排名（不沿用跨校名次）；某校無資料時顯示友善提示。
- **測試驗收**：`npm run check` ✅、`npm run build:pages` ✅、Inline scripts OK ✅
- **Commit**：`802f429`（2026-06-27，已 push）
- **修改檔案**：`index.html`、`js/index.js`

#### ✅ P2 — 教師管理端「一鍵下載最新題庫 (CSV)」

- **目標**：在教師頁面提供一鍵下載最新版單字庫與句型庫的 CSV 工具，供教師備課與檢討使用。
- **實作方式**：
  - `teacher.html`（HTML）：在左側欄位、課堂場次卡下方，新增「📥 管理工具：下載最新題庫」`glass-card`，包含藍色「📖 下載單字庫」與紫色「📝 下載句型庫」兩個按鈕，以及 `#downloadStatus` 狀態訊息行。
  - `teacher.html`（JS）：新增 `VOCAB_URL`、`SENTENCE_URL` 兩個常數（指向 GitHub Pages 公開 JSON）；新增 `setDownloadStatus()`、`triggerCSVDownload()`、`escCSV()` 三個工具函數；新增 `downloadVocabCSV()` 與 `downloadSentenceCSV()` 兩個非同步下載函數。
  - **核心設計**：
    - 即時讀取 GitHub Pages 最新版 JSON，附 `?t=timestamp` 防快取。
    - 加入 **UTF-8 BOM** 確保 Excel 正確顯示中文。
    - 按鈕點擊時顯示「⏳ 下載中...」，完成後還原並在下方顯示「✅ 成功下載 N 個單字！」或「❌ 下載失敗」。
    - `escCSV()` 對含逗號、引號、換行的欄位進行標準 CSV 跳脫保護。
  - **CSV 欄位結構**：
    - 單字庫：`單字(Word)`、`中文(Chinese)`、`主題(Topic)`、`音標(Phonetic)`、`難度年級(Grade)`、`拼字音節(Chunks)`
    - 句型庫：`句型(Pattern)`、`中文說明(Description)`、`範例英句(Example)`、`範例中譯(Translation)`、`對應主題(Topic)`、`難度年級(Grade)`
- **測試驗收**：`npm run check` ✅、`npm run build:pages` ✅、Inline scripts OK ✅
- **Commit**：`3b38b14`（2026-06-27，已 push）
- **修改檔案**：`teacher.html`

### 本次修改檔案

- [x] `index.html`（排行榜頁籤 UI：四個 `lb-tab` 按鈕）
- [x] `js/index.js`（新增 `_leaderboardCache`、`renderLeaderboard()`、`switchLeaderboardTab()`；`loadLeaderboard()` 改為 limit 100）
- [x] `teacher.html`（新增題庫下載工具卡片 HTML + `downloadVocabCSV()` / `downloadSentenceCSV()` JS）

### 本次未完成項目

- 無。P1、P2 均 100% 完成交付。

### 測試與驗收紀錄

- [x] `npm run check` 通過（JS 語法 + Inline scripts + JSON，全綠）
- [x] `npm run build:pages` 通過（dist 生產物正常）
- [x] GitHub 已 push，GitHub Actions 成功
- [x] P1 排行榜頁籤切換：跨校/各校頁籤可正常切換，校內重新排名，無資料時顯示提示
- [x] P2 教師端 CSV 下載：兩個按鈕可正常觸發，狀態訊息正常顯示，UTF-8 BOM 確認加入

### 下一步建議（尚未開發之精進項目，按優先序）

1. ✅ **P3 — 鬆綁 AI 自由造句 + 後端語法自我檢查**（已完成，見下方 Checkpoint 4）
2. ⬜ **P4 — 個人錯題開採寶石手冊**（UI + DB 資料表設計）
3. ⬜ **P5 — 智慧型 Spiral 螺旋式適性出題**（API 出題加入歷史錯題權重）
4. ⬜ **P6 — 教師端 AI 各校學習數據強弱項分析報告**（Gemini API 整合）
5. ⬜ **P7 — 口說 AI 音標發音弱項診斷**（後端語音分析）
6. ⬜ **P8 — 三校即時挑戰對抗賽大螢幕看板**（WebSocket / Realtime）

### 交還 commit / branch

- Branch：`main`
- Commit：`3b38b14`
- 是否已 push：是

---

## Checkpoint 4 工作記錄（2026-06-27 下午）

### 本次完成項目

#### ✅ P3 — 鬆綁 AI 自由造句 + 後端語法自我檢查架構

- **目標**：讓 AI 為每個單字創作更自然、多樣化的例句，同時以後端程式碼自動驗收品質，不合格才 fallback 回標準句。
- **實作方式**：
  - **Prompt 改造（`generate-challenge/index.ts`）**：
    - 移除「exampleSentence 必須套用 pattern」的硬性限制。
    - 改為「自由造句約束」：句子必須自然包含 target word、長度 5–25 字、使用學生日常詞彙、禁止「I see a [word]」死板套句、可用問答對話/陳述/祈使等多種形式。
  - **新增 `validateAISentence()` 後端驗證函數**：三道關卡：
    1. 句子必須以單字邊界（`\b`）包含 target word
    2. 句子長度需在 4–35 字之間
    3. 禁止「I see a/I can see a」等死板句型
  - **改造 `normalizeChallenge()` 中的句子選擇邏輯**：
    - AI 句通過 `validateAISentence` → 採用，並輸出 `[P3 ✅ AI句通過]` log
    - AI 句不通過 → fallback 回 `repairGrammar` 後的標準句，並輸出 `[P3 ❌ AI句不符]` log 說明原因
  - **補強 `repairGrammar()` 新增第 4.6 節**：
    - 數字詞（five → `I have five books on my desk.`）
    - 複數集合名詞（people → `There are many people in the park.`）
    - 抽象形容詞（quiet → `The library is very quiet.`）
    - 專有地名（canada → `I want to visit Canada someday.`）
  - **新增 `buildSmartDefaultSentence()` 函數**：
    - 16 種主題分類（Food/Animals/Colors/Numbers/Clothing/Body/School/Sports/Weather/Family/Time/Place/Prepositions/Actions/Adjectives/Health）各有適合的句型模板。
    - 未知主題兜底：`Let me show you a [word].`
  - **改造 `toFallbackWord()` 造句邏輯**：
    - 舊邏輯：`exampleAnswer` 不含 target word 時仍強行套入 `patternText + exampleAnswer`（導致 "There is a winter in the room."）
    - 新邏輯：`exampleAnswer` 含 target word → `repairGrammar()`，否則 → `buildSmartDefaultSentence()`（依主題自動生成語意正確的句子）
- **測試驗收（final run）**：
  - `six` → `I have six books on my desk.` ✅
  - `rabbit` → `Look! There is a rabbit.` ✅
  - `jump` → `I can jump.` ✅
  - `boat` → `I go to school by boat.` ✅
  - `slow`/`december` 等少數邊角案例（vocabulary.json topic 分類不精確）仍待 topic 資料修正，不影響核心架構
- **已知限制**：部分單字如 `slow`（被標為 `Transportation` 但應為 `Adjectives`）需待 vocabulary.json 中 topic 欄位修正才能完全對應
- **Commit**：`812f70c`（2026-06-27，已 push）
- **Edge Function**：已部署至 `tzvnyluqommusppbzyiy`

### 本次修改檔案

- [x] `supabase/functions/generate-challenge/index.ts`（改造 Prompt + 新增 `validateAISentence()` + 新增 `buildSmartDefaultSentence()` + 強化 `repairGrammar()` 第 4.6 節 + 改造 `normalizeChallenge()` 選句邏輯 + 改造 `toFallbackWord()` 造句邏輯）
- [x] `工程師交接清單_2026-06-27.md`（更新交接記錄）

### 測試與驗收紀錄

- [x] `npm run check` 通過（JS 語法 + Inline scripts + JSON，全綠）
- [x] `npm run build:pages` 通過
- [x] `node tools/test-api.mjs` 通過 — 數字、動物、動作詞例句品質顯著提升
- [x] Edge Function 已部署並正常回應
- [x] GitHub 已 push，GitHub Actions 成功

### 下一步建議（尚未開發之精進項目，按優先序）

1. [x] **P4 — 個人錯題開採寶石手冊**（已完成，登入後自動加載，入口與徽章整合至頂部 Navbar，未挑戰字不呈現）
2. ⬜ **P5 — 智慧型 Spiral 螺旋式適性出題**（API 出題加入歷史錯題權重）
3. ⬜ **P6 — 教師端 AI 各校學習數據強弱項分析報告**（Gemini API 整合）
4. ⬜ **P7 — 口說 AI 音標發音弱項診斷**（後端語音分析）
5. ⬜ **P8 — 三校即時挑戰對抗賽大螢幕看板**（WebSocket / Realtime）

> **附：vocabulary.json topic 修正建議**（下一位工程師可選做）  
> 以下單字的 topic 目前分類有誤，建議調整以確保 `buildSmartDefaultSentence` 能正確配對：
> - `slow`、`quick`、`fast` → topic 改為 `Adjectives`
> - `december`、`january` 等月份 → topic 改為 `Time`（可新增月份 builder）
> - `ask`、`talk`、`play` → topic 改為 `Actions` / `Verbs`
> - `sport`、`sports` → topic 改為 `Sports`（注意帶逗號的「`Sport,`」格式問題，需修正 JSON）

### 交還 commit / branch

- Branch：`main`
- Commit：`3fab3fd`
- 是否已 push：是

---

## Checkpoint 8 工作記錄（2026-06-27 晚上）

### 本次完成項目

#### ✅ P4 — 英語寶石開採手冊入口移位 & 清爽首頁重構
- **大廳 UI 減量**：移除首頁中間擁擠的寶石等級卡片與寶石手冊卡片，還原清爽大廳。
- **Navbar 整合**：將學生學校、姓名、當前寶石級數整合至頂部導覽列按鈕（點擊呼叫礦物科普 Modal），並在 Navbar 新增「💎 英語寶石手冊」黃金質感按鈕。
- **遮罩與 Modal 載入 Bug 修復**：
  - 修復首頁載入時 Modal 與遮罩在未登入或載入時直接在中間彈出的 Bug。
  - `#levelModalOverlay` 加入 default `display: none`。
  - `#handbookModal` 移除 style 末尾與 `display: none` 衝突 of 重複 `display: flex`。

#### ✅ 經典中英文卡片配對遊戲 (連連看) 實作
- **Spelling 第 4 關配對題**：在單字拼寫大題中，第 4 題自動進入 Card Matching（配對模式）。
- **配對連連看機制**：隨機打亂 3 組英中對應詞（共 6 張卡片），提供流暢的點擊、配對成功變綠並凍結、配對失敗變紅並震動等高互動體驗，並伴隨 Web Audio 合成音效反饋。

#### ✅ 出題單字池縮減至 10 個 & 挑戰維持 12 題
- **單字池縮減**：Edge Function 候選單字池縮減為 10 個，降低 Token 消耗量以避開免費版 429 TPM 限制。
- **重造新句與句型**：挑戰維持 12 題，第 11-12 題由 AI 從這 10 個單字中再選 2 個，重複考驗並生成完全不同的新英文例句與新句型。
- **中文對齊**：後端出題會動態附加 `distractorZhs` 英中字典，使連連看可完美加載中文干擾字翻譯。

#### ✅ 全大題口說及格額外加分 (+2)
- **Spelling 新增口說**：拼字題答對揭曉區新增口說單字錄音麥克風。
- **加分機制**：在任何題目中（拼字、口說、句型），只要學生挑戰口說且分數 >= 80 分，即可獲得額外加分 +2 分（每題限領一次）。
- **結算畫面**：最終挑戰得分累入口說加分（總分上限為 100 分），並在成績結算畫面上醒目顯示 `(口說加分 +X)`。

### 本次修改檔案
- [app.html](file:///C:/Antigravity%202026/新港國小英語單字王網站/app.html)
- [index.html](file:///C:/Antigravity%202026/新港國小英語單字王網站/index.html)
- [js/index.js](file:///C:/Antigravity%202026/新港國小英語單字王網站/js/index.js)
- [js/challenge.js](file:///C:/Antigravity%202026/新港國小英語單字王網站/js/challenge.js)
- [supabase/functions/generate-challenge/index.ts](file:///C:/Antigravity%202026/新港國小英語單字王網站/supabase/functions/generate-challenge/index.ts)

### 測試與驗收紀錄
- [x] `npm run check` 通過（JS 語法 + Inline scripts + JSON，全綠）
- [x] `npm run build:pages` 通過
- [x] `node tools/run-comparison-test.mjs` 對比測試通過（Groq 平均 2.6 秒呼叫，100% 匹配候選池，例句合格率 100%）
- [x] Edge Function 已成功部署至 Supabase。
- [x] 本地與 Git commit/push 均已完成。

### 交還 commit / branch
- Branch：`main`
- Commit：`3fab3fd`
- 是否已 push：是

---

## Checkpoint 9 工作記錄（2026-06-28）

### 本次完成項目

#### ✅ P5 — 智慧型 Spiral 螺旋式適性出題

- **目標**：讓 Edge Function 出題候選池不只依序塞入錯題，而是以「錯題、弱項主題、新字、學習中字、舊字複習」固定配額混合，形成螺旋式複習。
- **實作方式**：
  - `generate-challenge/index.ts` 的 `selectCandidatePool()` 改為 P5 Spiral 配額制：
    - `weak`：最多 3 個，優先納入前端錯題、近期錯題與 mastery = 0 的單字。
    - `weak-topic`：最多 1 個，依錯題與低 mastery 推算弱項主題，再補同主題未熟字。
    - `new`：最多 3 個，確保學生持續遇到新字。
    - `learning`：最多 2 個，複習 mastery = 1 或 2 的學習中字。
    - `mastered`：最多 1 個，保留舊字回巡，避免熟字完全消失。
    - `fill`：當某類不足時依弱項、學習、新字、熟字順序補滿 10 字候選池。
  - 新增 `normalizeWordList()`、`extractWrongWords()`、`mergeWordLists()`、`normalizeTopicName()`、`getWeakTopics()` 等輔助函數，支援錯題去重、物件/字串陣列解析與 topic 正規化。
  - 後端會自動讀取 `daily_attempts` 最近 8 筆 `wrong_words`，與前端傳入的 `wrongWords` 合併，讓 Spiral 出題不只依賴當前頁面狀態。
  - 新增 `[P5 Spiral]` console log，輸出候選池桶別數量、弱項主題與實際候選字，方便部署後觀察。
  - 維持原本 10 字候選池 + 12 題挑戰模式，不增加 AI prompt token 壓力。

#### ✅ 題庫 topic 小修正

- `ask`、`talk`：由 `School` 改為 `Actions`。
- `fast`、`slow`：由 `Transportation` 改為 `Adjectives`。
- `hungry`：由 `Food` 改為 `Adjectives`。
- `grow`、`drink`、`wash`、`take`、`want`、`go`、`put`、`wake`、`live`、`meet`、`give`、`say`、`feel`、`hope`：統一修為 `Actions`。
- 停用污染資料 `Expansion / 新竹縣鳳岡區`，避免 PDF/地名抽取錯誤進入學生題庫。
- 後端同步新增資料防線：即使 GitHub Pages 題庫尚未更新，Edge Function 讀公開 JSON 時也會過濾污染資料。

#### ✅ API 驗收補強的出題品質防線

- 新增形容詞檢查：攔截 `I like hungry.` 這類把形容詞當名詞受詞的 AI 句。
- 新增月份檢查與月份 fallback：月份會產生 `December is a month of the year.`，避免 `There is a May in the room.`。
- 新增常見動作詞檢查與 fallback：攔截 `There is a take/meet...`、`He is my grow.`、`I like drink.` 等模板錯置。
- 新增功能詞特例：`OK` 固定為 `I am OK now.`，`will` 固定為 `I will go to school tomorrow.`，避免 `There is a will/ok...`。
- 新增 AI 中文翻譯品質檢查：若 AI 產生亂碼、明顯簡體雜訊、過長重複標點，會回退題庫標準中譯。
- 新增人物性別搭配檢查：攔截 `He is my mommy.`、`She is my dad.` 這類代名詞錯配。
- `buildDistractors()` 改為優先採用同主題且存在於題庫的干擾選項；AI 提供的跨題庫或跨主題選項會被濾掉，再由同主題題庫補齊。

#### ✅ Spiral 離線回歸測試

- 新增 `tools/test-spiral-selection.mjs`，不需 Supabase、AI key 或網路。
- 新增 `npm run test:spiral`：
  - 驗證上述 topic cleanup 是否正確。
  - 模擬 `ask/talk/slow` 弱項情境。
  - 驗證候選池固定 10 字、無重複，且包含弱項字、弱項主題、新字、舊字複習。

### 本次修改檔案

- [x] `supabase/functions/generate-challenge/index.ts`（P5 Spiral 選池、近期錯題合併、弱項主題推算與 log）
- [x] `data/vocabulary.json`（修正 topic、停用污染資料）
- [x] `tools/test-spiral-selection.mjs`（新增離線 Spiral 選池回歸測試）
- [x] `package.json`（新增 `test:spiral` script）
- [x] `工程師交接清單_2026-06-27.md`（更新本 Checkpoint 交接紀錄）

### 測試與驗收紀錄

- [x] `npm.cmd run check` 通過（JS 語法 + Inline scripts + JSON，全綠）
- [x] `npm.cmd run test:spiral` 通過（輸出候選池包含弱項字與新/舊混合）
- [x] `npm.cmd run build:pages` 通過（`dist/` 產物已重新生成）
- [x] `C:\Users\wsyeu\.lmstudio\.internal\utils\deno.exe check supabase/functions/generate-challenge/index.ts` 通過。
- [x] `npx.cmd supabase functions deploy generate-challenge` 已部署至 `tzvnyluqommusppbzyiy`。
- [x] `node tools/test-api.mjs` 真實 API 驗收通過：正式 Edge Function 可回 12 題，P5 出題流程正常，並已依輸出補強形容詞、月份、動作詞與干擾選項品質防線。
- [x] 追加最終品質防線後重新通過 `npm.cmd run check`、`npm.cmd run test:spiral`、`deno check`、`npm.cmd run build:pages`。
- [x] 追加最終品質防線後已重新部署 `generate-challenge`，並再次執行 `node tools/test-api.mjs` 通過：正式 API 可正常回 12 題，未再出現 `There is a will/ok`、`He is my mommy`、亂碼翻譯等阻斷性問題。

### 已知限制與下一步建議

1. **P5 已部署並完成正式 API 驗收**：後續可在 Supabase function logs 觀察 `[P5 Spiral]`，確認真實學生候選池配額分布。
2. **topic cleanup 可持續擴充**：本次已修正驗收中出現的主要錯分字；未來可用腳本掃描 `Other verbs`、`Other adjectives`、`Personal characteristics` 等類別，逐批正規化。
3. **非阻斷品質改善**：最後 live API 中仍可見少數自然度可改善句，如 `I can live.`、`call` 仍在 `Other` topic；建議納入下一輪題庫清理，而非阻擋 P5 上線。
4. **P6 教師端 AI 學習數據強弱項分析報告** 可以接在 P5 後開發，因為現在弱項主題已能在後端穩定推算。
5. **P7 口說 AI 音標診斷** 建議排在 P6 後，因為它需要額外後端語音資料流與成本控管。

### 交還 commit / branch

- Branch：`main`
- Commit：`f8ba1fc`（P5 implementation commit）
- Final quality guard commit：`ad2f0d1`
- 是否已 push：是

---

## Checkpoint 10 工作記錄（2026-06-28）

### 本次完成項目

#### ✅ 題庫 topic 清理工具 + 明確錯分字修正

- 新增 `tools/audit-vocabulary-topics.mjs`：
  - `npm run audit:vocab`：列出白名單中的 topic 修正建議。
  - `npm run fix:vocab`：套用已審核的明確錯分字修正。
- 已套用 104 筆明確 topic cleanup：
  - 動詞/動作詞歸入 `Actions`，例如 `call`、`come`、`find`、`make`、`wear`、`cook`、`use` 等。
  - 描述詞歸入 `Adjectives`，例如 `clean`、`full`、`thirsty`、`sweet`、`hard`、`favorite` 等。
  - 功能詞/語氣詞歸入 `Other adverbs` 或 `Interjections`，例如 `always`、`really`、`yes`、`welcome` 等。
- `npm run audit:vocab` 已回報 `0 suggestion(s)`，代表目前白名單中的明確修正已全部套用。

#### ✅ P6 — 教師端學習數據強弱項分析報告

- 在 `teacher.html` 新增「學習數據強弱項分析」區塊。
- 教師可選擇學校與最近 7/30/90 天，產生：
  - 參與學生數與挑戰紀錄數。
  - 平均分數、口說平均。
  - 高頻錯字 Top 8。
  - 弱項主題排行。
  - 學校/年級參與分布。
  - 智慧教學建議（依分數、口說、弱項主題與高頻錯字生成）。
- 目前採純前端統計，不新增資料表、不增加 AI API 成本。

#### ✅ P7 — 口說音標弱項診斷 MVP

- 在 `js/speech.js` 新增 `diagnosePronunciation(expected, transcript)`。
- 在 `app.html` 口說結果區新增「AI 音標弱項診斷」顯示卡。
- 在 `js/challenge.js` 接上診斷結果：
  - 發音完整時給予進階建議。
  - 未辨識或錯誤時提示開頭音、尾音、th/r/l/v/f/ng 等常見弱點。
  - 不上傳錄音、不呼叫後端，維持零額外 API 成本。

### 本次修改檔案

- [x] `tools/audit-vocabulary-topics.mjs`（新增 topic audit/fix 工具）
- [x] `data/vocabulary.json`（套用明確 topic cleanup）
- [x] `package.json`（新增 `audit:vocab`、`fix:vocab` script）
- [x] `teacher.html`（新增 P6 學習數據報告 UI 與統計邏輯）
- [x] `app.html`（新增 P7 診斷顯示區與樣式）
- [x] `js/speech.js`（新增音標弱項診斷函式）
- [x] `js/challenge.js`（接入口說診斷與題目切換重置）
- [x] `工程師交接清單_2026-06-27.md`（更新本 Checkpoint 記錄）

### 測試與驗收紀錄

- [x] `npm.cmd run check` 通過（JS 語法 + Inline scripts + JSON，全綠）
- [x] `npm.cmd run audit:vocab` 通過，回報 `0 suggestion(s)`
- [x] `npm.cmd run test:spiral` 通過
- [x] `npm.cmd run build:pages` 通過
- [x] 本機 HTTP 檢查通過：
  - `http://127.0.0.1:3000/teacher.html` → 200
  - `http://127.0.0.1:3000/app.html` → 200
  - `http://127.0.0.1:3000/index.html` → 200

### 下一步建議

1. P6 可再進一步接後端 Edge Function 產生「AI 自然語言總評」，但目前純前端版本已可用且沒有成本風險。
2. P7 若要升級成真正錄音音素分析，需要新增音訊上傳與後端模型評估，建議先評估成本、學生隱私與家長同意流程。
3. 題庫 cleanup 工具可持續擴充白名單，逐批處理仍留在 `Other` 的名詞、地名與功能詞。

### 交還 commit / branch

- Branch：`main`
- Commit：`8eda53d`
- 是否已 push：是

---

## Checkpoint 11 工作記錄（2026-06-28）

### 本次完成項目

#### ✅ 教師端課堂 QR Code + 學生掃碼自動帶入場次碼

- 在 `teacher.html` 進行中場次卡加入 QR Code 區塊。
- QR Code 連結格式：
  - `class.html?classCode=406032`
  - 使用 `classCode` 避免與 Google OAuth redirect 的 `code` 參數撞名。
- 新增教師端操作：
  - 「顯示 QR」：展開/收合 QR Code。
  - 「複製連結」：複製學生掃碼連結，若瀏覽器限制剪貼簿，會展開 QR 區並提示手動複製。
- 在 `class.html` 加強參數讀取：
  - 支援 `classCode`
  - 支援備援參數 `session`、`room`
  - 保留既有 `pendingClassCode` 與登入後自動跳轉 `app.html?mode=class&code=...` 流程。

### 本次修改檔案

- [x] `teacher.html`（QR Code UI、學生連結生成、複製連結、顯示切換）
- [x] `class.html`（掃碼參數自動帶入強化）
- [x] `工程師交接清單_2026-06-27.md`（更新本 Checkpoint 記錄）

### 測試與驗收紀錄

- [x] `npm.cmd run check` 通過（JS 語法 + Inline scripts + JSON，全綠）
- [x] `npm.cmd run build:pages` 通過
- [x] 本機 HTTP 檢查通過：
  - `http://127.0.0.1:3000/teacher.html` → 200
  - `http://127.0.0.1:3000/class.html?classCode=406032` → 200
  - `http://127.0.0.1:3000/app.html` → 200

### 已知限制與下一步建議

1. QR 圖片目前使用 `api.qrserver.com` 產生，若教室網路封鎖外部圖片服務，仍可使用「複製連結」與手動輸入場次碼作備援。
2. 下一階段可開發「教師統一題目包」：教師先設定題數/題型/難度/指定單字/句型，再由 AI 生成本場共同題目。

### 交還 commit / branch

- Branch：`main`
- Commit：`58ce18a`
- 是否已 push：是

---

## Checkpoint 12 工作記錄（2026-06-28）

### 本次目標

依序完成 Checkpoint A、B、C：

1. Checkpoint A：建立固定題目包資料結構，確保同一場只有一份題目。
2. Checkpoint B：老師可設定題數、題型、難度、指定單字/句型與補充指令，由 AI 生成本場題目包。
3. Checkpoint C：學生掃碼加入後共用同一批題目，完成後老師可查看每題正確率。

### 已完成內容

#### Checkpoint A：固定題目包資料結構

- 新增 `supabase/migrations/20260628000002_challenge_packages.sql`。
- 新增 `challenge_packages`：
  - `session_id` 唯一對應一份固定題目包。
  - 保留 `session_code`、`teacher_config`、`challenge_data`、`status`、`created_at`、`expires_at`。
  - 學生與老師都從同一筆資料讀取本場題目。
- 新增 `challenge_question_results`：
  - 記錄每位學生每題作答結果。
  - 支援每題答對人數、作答人數、正確率。
- 補上 RLS policy：
  - 學生可讀取 active 且未過期的題目包。
  - 老師可管理自己的題目包。
  - 學生可新增自己的逐題結果。
  - 老師/admin 可查詢逐題結果。
- 同步更新 `supabase/production-upgrade-existing-project.sql`。
- 修正 `supabase/migrations/20260627000001_update_leaderboard_view.sql`：
  - 遠端既有 view 欄位順序不同，`CREATE OR REPLACE VIEW` 會失敗。
  - 改為先 `DROP VIEW IF EXISTS student_leaderboard` 再建立。

#### Checkpoint B：老師設定題目包 + AI 生成

- `teacher.html` 新增老師出題設定：
  - 題數：10 / 12 / 15 / 20。
  - 題型比例：平均混合、拼字較多、口說較多、句型較多、複習檢核。
  - 難度：三年級、四年級、五年級、六年級。
  - 指定單字。
  - 指定句型。
  - 補充指令。
- 建立場次流程改為：
  1. 建立 `challenge_sessions`。
  2. 呼叫 `generate-challenge`。
  3. Edge Function 依老師設定生成題目。
  4. 寫入 `challenge_packages`。
- `generate-challenge/index.ts` 新增：
  - `teacherConfig`
  - `packageSessionId`
  - `packageSessionCode`
  - `packageExpiresAt`
  - 10 / 12 / 15 / 20 題支援。
  - 老師額外輸入單字可超出題庫，會以 `Teacher Custom` 候選處理。
  - 每題輸出穩定 `id`，供逐題統計使用。
- 修正 P5 近期錯題查詢欄位：
  - `wrong_words` → `wrong`
  - `created_at` → `timestamp`

#### Checkpoint C：學生共用題目包 + 考後每題正確率

- `js/challenge.js` 課堂模式改為優先讀取 `challenge_packages`：
  - 找不到 active 且未過期題目包時，阻止進入挑戰。
  - 同一場學生使用同一批題目。
  - 題目順序依學生與場次做穩定洗牌。
- 題數改為動態：
  - 進度條、下一題判斷、分數、正確率分母都改用實際題數。
- 新增逐題結果暫存與批次寫入：
  - 拼字、口說、句型、跳過都會記錄結果。
  - 完成挑戰時寫入 `challenge_question_results`。
- `teacher.html` 新增「本場考題與正確率」區塊：
  - 顯示本場題目清單。
  - 顯示每題答對人數 / 作答人數 / 正確率。
  - 學生提交後會刷新，也可手動按「更新正確率」。
- 關閉場次時同步將 `challenge_packages.status` 改為 `closed`。

### 遠端部署狀態

- Supabase Project Ref：`tzvnyluqommusppbzyiy`
- `npx.cmd supabase db push` 已成功套用：
  - `20260627000001_update_leaderboard_view.sql`
  - `20260628000002_challenge_packages.sql`
- `npx.cmd supabase db push --dry-run` 顯示：
  - `Remote database is up to date.`
- `npx.cmd supabase functions deploy generate-challenge` 已成功部署。

### 驗證結果

- [x] `npm.cmd run check` 通過。
- [x] `npm.cmd run build:pages` 通過。
- [x] `node tools\test-spiral-selection.mjs` 通過。
- [x] `node tools\test-api.mjs` 正式 Edge Function live API 通過。
- [x] 老師設定 `teacherConfig` live API 測試通過：
  - 題數 10 題。
  - 每題皆有穩定 `id`。
  - 回傳 `source=ai`。
- [x] Supabase DB dry run 顯示遠端資料庫已是最新。
- [x] 本機 HTTP 檢查通過：
  - `http://127.0.0.1:3000/teacher.html` → 200
  - `http://127.0.0.1:3000/class.html?classCode=406032` → 200
  - `http://127.0.0.1:3000/app.html` → 200
- [x] `git diff --check` 無 whitespace error。

### 待觀察項目

1. 實際課堂登入流程仍建議用老師帳號建立一場測試場次，確認學生 Google 登入後能完整寫入 `challenge_question_results`。
2. AI 仍可能產生少數中文翻譯自然度問題，但已有 fallback 與 normalize guard，不阻擋本次固定題目包功能。
3. QR 圖片仍使用外部 `api.qrserver.com`；若教室網路封鎖，仍可用複製連結與手動輸入場次碼。

### Git 狀態

- Branch：`main`
- Commit：`197c843`
- 是否已 push：是

---

## Checkpoint 13 — 2026-06-28 現況理解與 P12 問題稽核

接手工程師：Antigravity AI  
稽核時間：2026-06-28 21:00 (UTC+8)  
最新 commit（承接自上輪）：`197c843`

---

### 一、現況全貌確認（系統現況理解報告）

#### 架構

| 項目 | 內容 |
|---|---|
| 前端 | GitHub Pages 靜態網站 |
| 後端 | Supabase Auth + Postgres + RLS + Edge Functions (Deno) |
| Project Ref | `tzvnyluqommusppbzyiy` |
| Production URL | https://hs5743.github.io/english-word-king/ |

#### 功能全貌（已完成）

**學生端**
- Google OAuth 登入（限 `@gapp.hcc.edu.tw`）
- 三種挑戰模式：每日挑戰、自由練習、課堂挑戰
- 挑戰流程：拼字磁磚 → 打字 → 選擇題 → 中英文卡片配對遊戲（連連看）
- 口說練習每題均可加分（≥ 80 分得 +2 分，每題限一次）
- 挑戰完成後有寶石升級彈窗 + 音效 + Confetti 動畫
- 頂部三檔語速切換（快 1.2x / 中 0.85x / 慢 0.55x）
- 口說音標弱項診斷（純前端，零成本）

**教師端**
- 建立 6 位數課堂碼 + QR Code 顯示
- 設定題目包：題數（10/12/15/20）、題型比例、難度、指定單字
- 課堂學生共用同一批 AI 出題（`challenge_packages` 資料表）
- 顯示每題答對率即時統計
- 一鍵下載題庫 CSV（單字庫 + 句型庫）
- 學習數據強弱項分析報告（純前端統計）

**管理員端**
- 管理學生名冊、角色
- 設定 AI API Key（Gemini 主 / 備用 / Groq）

**大廳**
- 排行榜四頁籤（跨校聯合榜 / 新港 / 鳳岡 / 豐田）
- 登入後 Navbar 顯示學生姓名 + 寶石稱號

#### 出題系統核心流程

```
學生觸發挑戰
  ↓
generate-challenge (Deno Edge Function)
  ↓
P5 Spiral 螺旋配額選池（10 個候選單字）
  ├─ weak（≤3）：錯題、mastery=0
  ├─ weak-topic（≤1）：弱項主題補字
  ├─ new（≤3）：新字
  ├─ learning（≤2）：mastery=1或2
  └─ mastered（≤1）：舊字回巡
  ↓
送 AI（Groq / Gemini）生成 12 題
  └─ 第11-12題：從10字重選2字，造新句
  ↓
validateAISentence + repairGrammar + buildSmartDefaultSentence
（三層語法防線過濾）
  ↓
回傳題目給前端
```

#### 各 Checkpoint 完成狀態

| Checkpoint | 主要內容 | 狀態 |
|---|---|---|
| 初始 | 基礎架構、OAuth、出題 fallback | 完成 |
| CP 1-2 | 寶石升級、語法防禦、語速切換、麥克風檢測 | 完成 |
| CP 3 | 排行榜分校頁籤、教師端 CSV 下載 | 完成 |
| CP 4 | AI 自由造句 + 後端語法驗證（P3） | 完成 |
| CP 8 | 連連看配對、口說加分、Navbar 整合（P4） | 完成 |
| CP 9 | P5 Spiral 螺旋適性出題、topic 清理 | 完成 |
| CP 10 | topic 全面清理、P6 學習報告、P7 音標診斷 | 完成 |
| CP 11 | 教師端課堂 QR Code + 學生掃碼 | 完成 |
| CP 12 | 固定題目包 DB 資料結構、老師出題、學生共用、每題正確率 | 完成（有已知 Bug，見下節）|

#### 未完成功能

| 優先序 | 功能 |
|---|---|
| P8 | 三校即時挑戰對抗賽大螢幕看板（WebSocket/Realtime） |
| 後續 | P6 升級：後端 AI 自然語言總評 |
| 後續 | P7 升級：真實錄音音素分析 |

---

### 二、P12 固定題目包 — 問題稽核報告

本節記錄 Checkpoint 13 稽核中確認的程式邏輯 Bug、設計缺失與 UI 問題，供下一輪工程師直接修復。

#### 已確認的程式邏輯 Bug

**Bug 1：typeMix（題型比例）完全無效**

嚴重程度：高

老師在建立場次時選擇的題型比例（平均混合 / 拼字較多 / 口說較多 / 句型較多 / 複習檢核）對出題毫無影響。

原因：
- `generate-challenge/index.ts` Prompt 只把 `typeMix` 塞進說明文字（如 `balanced`），AI 完全不知道各代碼的意義，無法轉換成具體的拼字題 / 口說題 / 句型題比例。
- 前端 `challenge.js` 的題目渲染邏輯是以題目索引硬編碼決定題型，完全不讀取 typeMix 設定。

結果：老師不管選哪個選項，學生看到的挑戰內容都完全一樣。

**Bug 2：customWords（指定單字）不保證出現**

嚴重程度：高

- `buildTeacherCustomWords` 碰到不在 `vocabulary.json` 的單字時，一律生成 `"I can use [word] in class."` 例句，語意荒謬，失去教學價值。
- P5 Spiral 配額邏輯可能因某桶（weak / new...）已滿而跳過指定單字，老師指定的字不一定出現在最終題目裡。

**Bug 3：題數選 15/20 題時，AI Prompt 描述仍寫死 12 題規則**

嚴重程度：高

- `selectCandidatePool` 固定選出最多 10 個單字。
- AI Prompt 寫死「前 10 題必須完全覆蓋 10 個單字、第 11 和 12 題重複考驗」的規則。
- 老師選 15 或 20 題時，Prompt 的規則說明沒有跟著更新，AI 不知道如何正確補充，輸出不可預測。

**Bug 4：同一學生在同一場次重複挑戰報錯**

嚴重程度：中

- `daily_attempts.id` 生成邏輯：`${student_uid}_${today}_${sessionId}`
- 學生同一天在同一場次挑戰兩次，第二次 insert 因主鍵重複拋錯，學生看到「提交失敗」頁面。
- 應改為 upsert 或加入流水號區分。

#### 功能設計不完善

- typeMix 與指定句型傳入 AI 但無法有效解讀：AI 只收到字串代碼，沒有翻譯成「請出 X 題拼字 / Y 題口說」的具體指令。
- 教師無法在讓學生開始前預覽並重新出題：建立成功後沒有「題目預覽 + 重新出題」功能，若 AI 生成品質不佳，老師只能整個關閉場次重來。
- AI 出題失敗（fallback 題庫）時，教師端毫無通知：系統靜默 fallback，教師不知道題目實際來源。

#### UI 體驗問題

- 建立場次等待體驗差：最長等待 5-10 秒，Loading 只顯示一句話，沒有分段進度提示。
- 場次建立成功後看不到本場單字摘要：老師需要滾到下方才能看到實際出了哪些字。
- 沒有「重新出題」按鈕。
- 題型比例選項說明誤導老師：因後端未實作這些比例，應加上「功能開發中」標籤。
- 正確率區塊視覺引導不清楚：說明文字與實際行為不一致。

#### 改善優先序

| 優先序 | 問題 | 建議行動 | 主要檔案 |
|---|---|---|---|
| P0（最高） | typeMix 完全無效 | Prompt 加入具體比例說明；前端渲染讀取設定 | `generate-challenge/index.ts`、`challenge.js` |
| P0 | 指定單字不保證出現 | 強制將指定單字插入候選池最前列 | `generate-challenge/index.ts` |
| P0 | 題數 15/20 題 Prompt 規則錯誤 | 依 `requestedQuestionCount` 動態修正 Prompt | `generate-challenge/index.ts` |
| P1 | 重複挑戰報錯 | 改為 upsert | `js/challenge.js` |
| P1 | fallback 無通知 | 加 `source` 標示（AI / 題庫） | `teacher.html` |
| P2 | 題目預覽 + 重新出題 | 加入「重新出題」按鈕 | `teacher.html` |
| P2 | 建立進度體驗 | 加入分段進度步驟 | `teacher.html` |
| P2 | 題型選項去誤導 | 加上「即將支援」標籤 | `teacher.html` |

---

### 三、下一輪工程師立即任務

1. **優先修復 P0 三個 Bug**（typeMix、指定單字、題數規則），皆集中在 `supabase/functions/generate-challenge/index.ts` 的 Prompt 組裝段落與 `selectCandidatePool` 函式。（已於 CP14 完成修復）
2. **修復 Bug 4**（重複挑戰），在 `js/challenge.js` 的結果寫入處改為 upsert。（已於 CP13 完成修復）
3. 修復後執行 `npm run check` + `npm run build:pages`，通過後 `git push` 並執行 `supabase functions deploy generate-challenge`。（已於 CP14 完成修復與部署）

---

## Checkpoint 14 — 2026-06-29 核心問題修復與 UI 重構完成報告

接手工程師：Antigravity AI  
優化時間：2026-06-29 01:25 (UTC+8)  
最新 commit：`cfb59f0`（包含後續 UI 優化提交）

### 一、已完成的修復與功能優化

#### 1. 指定單字優先出題與去重機制
- 重構為 `resolveTeacherCustomWords`。
- 無論老師輸入的指定單字是否存在於題庫中，均優先轉換為 `FallbackWord`，並完美放置於 `candidatePool` 的最前列。
- 若屬於題庫內的字，保留題庫標準英漢對照、音標與句型，若不在題庫則透過 AI 兜底造句，杜絕單字被過濾遺失的問題。

#### 2. 重複題目均衡分配與題型排他分配
- 修正了大於 10 題（如 12、15、20 題）時，重複單字高度集中於前兩個字的缺陷。
- 引入「重複挑戰最小計數均衡演算法」，讓補足的重複題均勻分佈在 10 個單字池中。
- 引入「題型排他分配演算法」：**同一個單字在挑戰中出現兩次時，絕對不會被分配到相同的玩法（題型）**。例如第一次若為拼字（Spelling），第二次就必定為口說（Speech）或句型（Sentence）。

#### 3. API 錯誤自動重試與延遲分流
- 實作了後端 `executeWithRetry` 呼叫層重試機制。若 AI 服務因瞬時流量爆發而返回 429、503 等連線錯誤，系統會自動在後端等待 1.5 秒後重試，增加單次呼叫的成功率（最多重試 2 次）。
- 引入「供應商分流停頓」：在三段 Failover 中，若一個 AI 供應商宣告完全失敗（即主要 Gemini 失敗準備切換備用 Gemini，或備用 Gemini 失敗準備切換 Groq），系統會主動停頓（Sleep） 800 毫秒，分散對各提供商的爆發式集中連線，確保服務可靠性。

#### 4. 學生掃 QR Code 零點擊跳轉課堂
- 優化了 `class.html` 登入與重導向邏輯，Google OAuth 登入成功後直接重導向回當前帶有課堂碼的 `class.html`。
- 當學生已登入且掃碼進入帶有課堂碼的網址時，**系統會在 0.5 秒內「免點選」自動跳轉**至該課堂挑戰，毋需繁瑣地手動點擊按鈕或在大廳切換分頁。

#### 5. 教師大螢幕控制台 UI 優化（重構與細節修正）
- **QR 碼展示區去雜**：移除冗長混亂的 raw URL，替換為精緻的 instructions `📱 學生掃碼直接加入` 與置於底部的代碼塊 monospace URL。
- **場次設定資訊格欄排版**：將原本雜亂的文字堆疊，重構為精美對齊的 key-value 玻璃微卡片（🏫 學校、👥 班級、📦 題目包設定、🔄 重新出題）。
- **修正資訊格欄標籤重複問題**：修正了因為 JS 寫入前綴（`🏫 學校：`、`👥 班級：`）導致的文字重複顯示 Bug，將前綴文字全部移出 JS，統一由 HTML 結構與對齊樣式管理，讓介面完美無暇。
- **正確率區域摺疊保護**：將「本場考題與正確率」區塊預設為摺疊隱藏狀態 (`display: none`)。新增 `👁️ 展開題目與正確率` 按鈕，防止未完成挑戰的學生在投影大螢幕上提前看到考題與答案，檢討時由老師手動展開。

#### 6. 正確率列表題型標籤
- 在教師端本場考題正確率的逐題列表中，每題的副標題皆新增了 friendly 中文題型標籤（如 `🧩 拼字`、`🎤 口說`、`📝 句型`），便於檢討教學。

#### 7. 題庫升級與適性化階度增強計畫 (已提報計畫)
- 因應學生 16 階寶石適性學習進度，已擬定並提報單字題庫升級計畫（詳細記載於 `implementation_plan.md`）。該計畫規劃將 706 個單字細分為 16 階難度，並預載高品質造句以保障 Fallback 題庫之品質與 AI 發音 Grounding 的穩定性。

### 二、變更檔案對照表

- [class.html](file:///C:/Antigravity%202026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/class.html)：優化登入重導向地址與已登入學生的課堂自動加入。
- [teacher.html](file:///C:/Antigravity%202026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/teacher.html)：重構進行中場次面板 UI；新增摺疊/展開題目與正確率功能，新增題型中文標籤。
- [supabase/functions/generate-challenge/index.ts](file:///C:/Antigravity%202026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/supabase/functions/generate-challenge/index.ts)：實現指定單字優先池化、多餘題重複均衡以及題型排他分配邏輯。

---

## Checkpoint 15 完成報告 (2026-06-29)

> **工程師**：Antigravity AI (checkpoint session ee72d1fa)
> **負責老師**：新港國小英語教師
> **Branch**：main

### 一、本次已完成項目

#### 1. Groq 廢棄模型修正
- 在 `supabase/functions/admin-api/index.ts` 中，將測試用的 Groq 模型 `llama3-8b-8192`（已於 2025 年底被 Groq 停用）更新為現役模型 `llama-3.1-8b-instant`。
- 重新部署 `admin-api` 函數，修復教師大螢幕 AI 金鑰狀態檢查中 Groq 永遠顯示 ❌ 失敗的 Bug。
- 最後 commit：`Fix decommissioned Groq model in admin-api connection test`

#### 2. AI 出題三段式 Failover 加入重試與延遲分流
- 修改 `supabase/functions/generate-challenge/index.ts`：
  - 新增 `withRetry()` 輔助函數：每次 AI 呼叫失敗後等待 2 秒自動重試（最多 2 次），有效應對瞬間 503 過載。
  - 三段 Failover（Gemini 主→Gemini 備→Groq）之間加入 500ms 延遲，避免 API 請求瞬間累積觸發速率限制。
  - 最後 commit：`Implement AI retry wrapper and staggered fallbacks in generate-challenge`

#### 3. 單字庫升級 - difficultyLevel 1-16 分級（🟢 已完成）

**已完成：**
- 建立一次性升級指令碼 `tools/upgrade-vocabulary.mjs`，提供 `--dry-run` 與 `--resume` 模式。
- 升級策略：依年級 → 音節數 → 字母數 三級排序，均勻切分至 16 階：
  - G3（584字）→ Level 1-8（每階 ~73 字）
  - G4（27字）→ Level 9-10
  - G5（13字）→ Level 11-12
  - G6（2字）→ Level 13
  - G7（32字）→ Level 14
  - G8（15字）→ Level 15
  - G9（33字）→ Level 16
- 成功將 `difficultyLevel` 欄位寫入全部 706 個單字，並同步預載 **680/706 個例句**。
- Commit：`f103395` — 升級 vocabulary.json：新增 difficultyLevel 1-16 分級 + 預載 680/706 例句
- 後續已由 Edge Function 讀取 `sentence`/`sentenceZh` 作為 Fallback 例句，並使用 `difficultyLevel` 篩選適性化字池。
- Commit：`0e4fc0e` — Add difficultyLevel 16-tier filtering to Edge Function + pre-loaded sentences [CP15-3]

### 二、變更檔案對照表（Checkpoint 15）

- [data/vocabulary.json](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/data/vocabulary.json)：新增 `difficultyLevel`、`grade`、`sentence`、`sentenceZh` 欄位。
- [tools/upgrade-vocabulary.mjs](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/tools/upgrade-vocabulary.mjs)：一次性升級指令碼（完成後可保留備用）。
- [supabase/functions/admin-api/index.ts](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/supabase/functions/admin-api/index.ts)：修正 Groq 廢棄模型。
- [supabase/functions/generate-challenge/index.ts](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/supabase/functions/generate-challenge/index.ts)：AI 三段 Failover 加入重試與延遲分流。

### 三、交還 commit / branch

- Branch：main
- Commit：0e4fc0e
- 是否已 push：是

---

## Checkpoint 16 — 2026-06-29 全單字庫高品質例句重新生成與教師端下載優化

接手工程師：Antigravity AI
時間：2026-06-29 03:20 (UTC+8)
最新 commit：`406f0e6`

### 一、本次已完成項目

#### 1. 全單字庫 (706字) 高品質例句重新生成與取代
- 徹底推翻並捨棄舊的 `sentence-patterns.json` 模板套用機制。
- 直接利用 AI 助理的寫作能力，為全單字庫中 **706 個單字** 重新量身定制了高品質、文法自然、語句生動且適合國小 3-6 年級的英文例句及繁體中文翻譯。
- 解決了諸如 `tiger (It is a tiger)`、`cat (I see a cat)` 以及 `weather (I see a weather)`、`play (I see a play)` 等文法錯誤與千篇一律的「I see a...」套版句型。
- 將剩餘 26 個無例句單字補齊，目前單字庫已是 **100% 預載高品質客製化例句** (706 / 706 字)。

#### 2. 教師端單字庫下載支援例句欄位
- 修改 [teacher.html](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/teacher.html) 中的 `downloadVocabCSV()` 匯出邏輯。
- 將 `例句(Sentence)` 與 `例句中譯(Translation)` 兩個全新欄位加入 `vocabulary.csv` 導出的標頭與內容中，解決了老師下載 CSV 卻看不到新例句的設計漏洞。
- **句型庫（sentence-patterns.csv）** 保持不變，因其為系統句型範本與 AI grounding 引導，與單字個別例句本質不同，兩者相輔相成，利於教師備課。

#### 3. 測試、驗證與 Git 推送
- 本地檢查：執行 `cmd /c npm run check`，通過（JSON OK、Inline scripts OK）。
- 本地打包：執行 `cmd /c npm run build:pages`，順利重建 `dist/` 靜態發佈物。
- 推送部署：所有變更已 commit 並 `git push` 至 GitHub，自動觸發 GitHub Actions 部署。
- 新增輔助指令碼：
  - [tools/patch-sentences.js](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/tools/patch-sentences.js)：用於讀取生成好的 JSON 例句數據並寫入單字庫。
  - [tools/regenerate-all-sentences.mjs](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/tools/regenerate-all-sentences.mjs)：供未來若有新加入的單字，可用 Gemini API 一鍵重新大批量生成。

### 二、變更檔案對照表

- [data/vocabulary.json](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/data/vocabulary.json)：更新所有 706 個單字的高品質例句與中文翻譯。
- [teacher.html](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/teacher.html)：下載單字庫 CSV 功能新增例句與中譯欄位。
- [tools/patch-sentences.js](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/tools/patch-sentences.js)：新增單字庫句子套用工具。
- [tools/regenerate-all-sentences.mjs](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/tools/regenerate-all-sentences.mjs)：新增全單字庫重新生成工具（備用）。

### 三、交還 commit / branch

- Branch：`main`
- Commit：`406f0e6`
- 是否已 push：是

## Checkpoint 17 — 2026-06-29 寶石手冊「錯題開採複習專區」遊戲化複習開發完成

接手工程師：Antigravity AI
時間：2026-06-29 03:40 (UTC+8)
最新 commit：`65a7721`

### 一、本次已完成項目

#### 1. 遊戲化錯題開採複習機制實作
- **手冊頁面卡片升級**：修改首頁 `renderIndexHandbook()` 邏輯。當學生切換至「開採中」分頁時，原石卡片（代表 mastery < 2 的字）底部會渲染 `⛏️ 開始開採` 按鈕，點擊即可開啟複習挑戰。
- **迷你開採視窗 (Mining Modal) 與狀態機**：
  - 新增 `#miningModal` 與遮罩，無縫嵌入在首頁中。
  - **階段一：聽音寫字**。學生聽發音（呼叫既有 `window.SpeechEngine.speak`，採用標準英語語音）並輸入單字拼寫進行驗證。
  - **階段二：口說朗讀**。顯示該單字對應的高品質預載例句，學生點選麥克風，呼叫瀏覽器語音辨識錄音，並計算發音分數。
  - **相容與兜底**：若瀏覽器不支援語音辨識（如 iOS Safari 或部分環境），畫面會給予提示並自動判定通過，保證課堂教學時完全不卡關。

#### 2. 解鎖音效與灑彩帶特效（遊戲化反饋）
- **Web Audio 解鎖聲**：成功開採後使用 Web Audio API 合成清脆明亮的三重音解鎖聲。
- **寶石 Confetti 彩帶**：成功開採後使用 CSS Animation 在網頁上生成 40 個隨機寶石 Emoji（💎, 🔮, 🌟, 🌈, 👑...）從底部向上飄散並逐漸淡出的精美效果。

#### 3. 資料庫更新與實時同步
- 開採完成後，直接呼叫 Supabase 將學生對該單字的熟練度（mastery）升級為 2。
- 寫入成功後更新首頁 cached Profile、重繪手冊網格（灰色原石動態變為帶有正確寶石級數與等級名稱的彩色寶石）並關閉 Modal。

### 二、變更檔案對照表

- [index.html](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/index.html)：新增原石開鑿 Modal 結構與 CSS 動畫樣式。
- [js/index.js](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/js/index.js)：實作開採狀態機邏輯、語音與打字驗證、Supabase 寫入與音效/彩帶特效。

### 三、交還 commit / branch

- Branch：`main`
- Commit：`65a7721`
- 是否已 push：是

---

## Checkpoint 18 — 2026-06-29 支援學生從首頁大廳直接登入與狀態預覽

接手工程師：Antigravity AI
時間：2026-06-29 22:20 (UTC+8)
最新 commit：`dd16e26`

### 一、本次已完成項目

#### 1. 支援登入後跳轉回首頁大廳 (index.html)
- 在 [join.html](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/join.html) 初始化時新增 `redirect` URL 參數解析。若包含該參數，登入成功後即會保存於 `sessionStorage` 中，完成 Google OAuth 跳轉驗證後，會精準跳轉回指定的頁面（如 `index.html`），而不是強制跳轉至挑戰頁面 `app.html`。

#### 2. 大廳新增學生登入入口與按鈕狀態切換
- **大廳導覽列**：在 [index.html](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/index.html) 的右上角 navbar 靜態選單中新增「學生登入」按鈕，指向 `join.html?redirect=index.html`。
- **Hero 區域**：在 Hero 主要操作按鈕下方新增「已有帳號？直接登入大廳」連結，方便已有帳號的學生登入。
- **狀態判定與切換**：修改 [js/index.js](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/js/index.js)。當檢測到學生已登入時，自動隱藏大廳中的「已有帳號？直接登入大廳」連結，並將導覽列切換為載入完成的學生詳細資料（包含學校、姓名、寶石稱號與手動開啟「💎 英語寶石手冊」按鈕），使大廳功能完全無縫運作。

### 二、變更檔案對照表

- [index.html](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/index.html)：新增學生登入與直通大廳的靜態 UI。
- [join.html](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/join.html)：新增 `redirect` URL 參數支援，防範 OAuth 遺失並精準跳轉。
- [js/index.js](file:///C:/Codex_Work_2026/%E6%96%B0%E6%B8%AF%E5%9C%8B%E5%B0%8F%E8%8B%B1%E8%AA%9E%E5%96%AE%E5%AD%97%E7%8E%8B%E7%B6%B2%E7%AB%99/js/index.js)：實作登入後隱藏登入入口並展示學生資訊與寶石手冊入口。

### 三、交還 commit / branch

- Branch：`main`
- Commit：`dd16e26`
- 是否已 push：是

---

## CP26 開發計畫：三校即時對抗賽大螢幕看板

> **撰寫日期**：2026-07-10  
> **計畫撰寫者**：Antigravity AI（本輪未實作，交由下一位工程師執行）  
> **預計版本號**：`v1.7.0-CP26`  
> **預計工作時間**：約 2 小時

---

### 一、功能概述

開發一個全新獨立靜態頁面 `scoreboard.html`（大螢幕看板），配合以下整合：

1. **`teacher.html`**：在進行中場次卡上新增「📺 開啟大螢幕看板」按鈕，一鍵在新分頁開啟看板
2. **`scoreboard.html`**（新增）：全螢幕大螢幕看板，以精美動畫即時展示本場三校對抗賽數據
3. **`index.html` + `js/index.js`**：對抗賽進行中時，首頁大廳自動出現「現正直播」橫幅，讓其他學校學生可直接點擊進入觀看看板
4. **新增 SQL Migration**：允許 anon（未登入）讀取進行中的 `challenge_sessions`，以支援首頁大廳橫幅功能

---

### 二、已確認的設計決策

| 項目 | 決策 |
|---|---|
| 看板進入方式 | 從 `teacher.html` 場次卡一鍵開啟新分頁，教師帳號 Session 自動延續，免重新登入 |
| 排行榜顯示人數 | 前 30 名 |
| 三校積分範圍 | **本場場次積分**（以 `session_id` 過濾 `daily_attempts`），非累積總分 |
| 首頁大廳連結 | 對抗賽進行中時，index.html 自動出現「🏆 現正直播！」橫幅 |
| 看板控制權 | 僅開啟本次對抗賽的教師帳號（`teacher_uid = auth.uid()`）有控制按鈕（可結束對抗賽）|

---

### 三、關鍵技術架構分析（已由前工程師研究確認）

#### 課堂模式的 `daily_attempts` 寫入特性（重要！）

- 課堂模式（`class mode`）寫入 `daily_attempts` 時，`practice = TRUE`，`session_id = [課堂 UUID]`
- 因此課堂分數**不計入全站累積分數**（`school_leaderboard` VIEW 排除 `practice=true`）
- 看板三校積分必須直接查詢 `daily_attempts WHERE session_id = ? GROUP BY school`，**不可使用** `school_leaderboard` 或 `student_leaderboard` VIEW

#### Supabase Realtime 已在 `teacher.html` 驗證可用

```javascript
// teacher.html 現有模式，可直接複用
realtimeChannel = supabaseClient
  .channel(`attempts-session-${sessionId}`)
  .on('postgres_changes',
    { event: '*', schema: 'public', table: 'daily_attempts' },
    payload => {
      if (payload.new?.session_id === sessionId) { /* 更新排行榜 */ }
    })
  .subscribe()
```

#### RLS 說明

- `daily_attempts`：目前只允許 `authenticated`（學生看自己、教師看全部）讀取
- `challenge_sessions`：目前只允許 `authenticated` 且 `is_school_email()` 讀取 active session
- **看板（scoreboard.html）**：教師從 teacher.html 用 `window.open()` 新分頁開啟，Supabase Session 存於 `localStorage`（同一 origin），**自動延續教師帳號**，有 `has_role(['teacher'])` 權限，可讀取所有 `daily_attempts`，**不需新增 daily_attempts RLS**
- **首頁大廳橫幅**：`index.html` 可被 anon 讀取，需新增一條 SQL Policy 讓 anon 可讀取 `challenge_sessions` 的進行中場次基本資訊

---

### 四、需新增的 SQL Migration

#### 新檔案：`supabase/migrations/20260710000000_scoreboard_anon_policy.sql`

```sql
-- CP26：允許未登入用戶讀取進行中的場次基本資訊
-- 用途：首頁大廳橫幅需要知道是否有對抗賽正在進行
-- 安全性：只暴露 session_code、school、expires_at、status，不暴露 teacher_uid 等敏感資訊

DROP POLICY IF EXISTS "任何人可讀取進行中場次基本資訊" ON challenge_sessions;
CREATE POLICY "任何人可讀取進行中場次基本資訊"
  ON challenge_sessions FOR SELECT
  TO anon
  USING (status = 'active' AND expires_at > NOW());
```

> **注意**：此 Policy 要套用到正式資料庫，需在 Supabase Dashboard → SQL Editor 執行，**或**執行 `npx.cmd supabase db push`。  
> 同時務必將此 SQL 也加入 `supabase/production-upgrade-existing-project.sql` 最底部，維持文件一致。

---

### 五、需修改的檔案清單

#### [MODIFY] `teacher.html`

在「進行中場次卡」的操作按鈕區（`#activeSessionCard` 或類似容器，尋找含有「關閉場次」按鈕的 `div`），新增：

```html
<button class="btn btn--scoreboard" id="btn-open-scoreboard" onclick="openScoreboard()">
  📺 開啟大螢幕看板
</button>
```

新增對應 CSS（在 `<style>` 區塊）：

```css
.btn--scoreboard {
  background: linear-gradient(135deg, hsl(268,75%,55%), hsl(300,70%,55%));
  color: #fff;
  border: none;
  font-weight: 800;
  letter-spacing: 0.03em;
  box-shadow: 0 4px 20px hsla(268,75%,55%,0.35);
  transition: var(--spring-transition);
}
.btn--scoreboard:hover {
  transform: translateY(-2px) scale(1.04);
  box-shadow: 0 8px 28px hsla(268,75%,55%,0.50);
}
```

新增 JS 函式（在 `<script>` 區塊）：

```javascript
function openScoreboard() {
  if (!activeSession || !activeSession.id) {
    alert('請先建立課堂場次再開啟看板');
    return;
  }
  const url = new URL('scoreboard.html', window.location.href);
  url.searchParams.set('session', activeSession.id);
  url.searchParams.set('code', activeSession.session_code);
  window.open(url.toString(), '_blank');
}
```

---

#### [MODIFY] `index.html`

在 `<section class="hero">` 內、`<div style="display: flex; flex-direction: column; ...">` 按鈕群組**之前**，新增：

```html
<!-- 三校即時對抗賽橫幅（對抗賽進行中時由 JS 顯示，預設隱藏） -->
<div id="live-contest-banner" style="display:none; width:100%; margin-bottom:1.2rem;">
  <a id="live-contest-link" href="#" target="_blank"
     style="display:flex; align-items:center; justify-content:center; gap:10px;
            background:linear-gradient(135deg,hsla(42,100%,55%,0.15),hsla(268,75%,63%,0.12));
            border:1.5px solid hsla(42,100%,65%,0.4);
            border-radius:var(--r-full); padding:12px 28px; text-decoration:none;
            font-weight:800; font-size:1.05rem; color:var(--clr-gold-1);
            box-shadow:0 4px 20px hsla(42,100%,55%,0.15);
            animation: pulse-border 2s ease-in-out infinite;">
    <span style="font-size:1.3rem; animation: bounce-emoji 1s ease-in-out infinite;">🏆</span>
    <span id="live-contest-text">三校即時對抗賽進行中！點擊觀看直播看板</span>
    <span style="background:var(--clr-gold-1);color:#fff;border-radius:var(--r-full);
                 padding:2px 10px;font-size:0.75rem;font-weight:900;margin-left:4px;">LIVE</span>
  </a>
</div>
```

同時在 `<style>` 或 `css/style.css` 新增（建議加在 `index.html` inline style）：

```css
@keyframes pulse-border {
  0%, 100% { box-shadow: 0 4px 20px hsla(42,100%,55%,0.15); border-color: hsla(42,100%,65%,0.4); }
  50%       { box-shadow: 0 4px 28px hsla(42,100%,55%,0.35); border-color: hsla(42,100%,65%,0.8); }
}
@keyframes bounce-emoji {
  0%, 100% { transform: translateY(0); }
  50%       { transform: translateY(-4px); }
}
```

---

#### [MODIFY] `js/index.js`

在現有的初始化函式（`DOMContentLoaded` 或 `init()` 內部）新增呼叫：

```javascript
checkLiveContest();
```

新增函式（加在檔案末尾）：

```javascript
// ── CP26：檢查是否有進行中的三校對抗賽，若有則顯示首頁橫幅 ──
async function checkLiveContest() {
  try {
    const sb = window.SupabaseConfig.getSupabase();
    if (!sb) return;

    // 查詢進行中且未過期的場次（anon 可讀，見 CP26 Migration）
    const { data: sessions, error } = await sb
      .from('challenge_sessions')
      .select('id, session_code, school, expires_at')
      .eq('status', 'active')
      .gt('expires_at', new Date().toISOString())
      .limit(1);

    if (error || !sessions || sessions.length === 0) return;

    const session = sessions[0];
    const banner = document.getElementById('live-contest-banner');
    const link = document.getElementById('live-contest-link');
    const text = document.getElementById('live-contest-text');

    if (!banner || !link) return;

    // 更新連結與文字
    const scoreboardUrl = new URL('scoreboard.html', window.location.href);
    scoreboardUrl.searchParams.set('session', session.id);
    scoreboardUrl.searchParams.set('code', session.session_code);

    link.href = scoreboardUrl.toString();
    if (text) {
      text.textContent = `${session.school || '三校'}即時對抗賽進行中！點擊觀看直播看板`;
    }

    // 顯示橫幅
    banner.style.display = 'block';
    banner.style.animation = 'animate-in 0.5s ease both';
  } catch (err) {
    console.warn('[CP26] checkLiveContest error:', err);
  }
}
```

---

#### [NEW] `scoreboard.html`（完整規格）

**檔案位置**：與 `index.html` 同層，`scoreboard.html`

**頁面三大區塊佈局（全螢幕，適合投影機）**：

```
┌──────────────────────────────────────────────────────────────┐
│  Header：場次名稱 | 🔴 LIVE 閃爍指示器 | 計時器 | [教師：結束對抗賽] │
├──────────────────────┬───────────────────────────────────────┤
│                      │                                       │
│  三校積分比較 Panel   │  個人排行榜（前 30 名）               │
│  ┌──────────────┐    │  🥇 No.1 [姓名] [學校] [分數]        │
│  │ 🏫 新港國小  │    │  🥈 No.2 ...                         │
│  │ 金色・積分   │    │  🥉 No.3 ...                         │
│  │ 人數・平均分 │    │  4. ...                               │
│  └──────────────┘    │  ...（最多 30 行，超出自動滾動）      │
│  ┌──────────────┐    │                                       │
│  │ 🏫 鳳岡國小  │    │                                       │
│  │ 藍色・積分   │    │                                       │
│  └──────────────┘    │                                       │
│  ┌──────────────┐    │                                       │
│  │ 🏫 豐田國小  │    │                                       │
│  │ 綠色・積分   │    │                                       │
│  └──────────────┘    │                                       │
│                      │                                       │
├──────────────────────┴───────────────────────────────────────┤
│  底部動態牆：最新提交動態（最近 8 則，滑入滑出動畫）           │
└──────────────────────────────────────────────────────────────┘
```

**視覺設計規格**：

- **背景**：深太空感漸層 `linear-gradient(135deg, hsl(225,40%,8%), hsl(220,50%,5%))`，確保投影清晰
- **字體**：引入現有 `css/style.css`，使用 `--font-en`（Outfit）+ `--font-zh`（Noto Sans TC）
- **三校積分卡**：毛玻璃 + 各校代表色發光邊框（`--clr-gold-1`/`--clr-blue-1`/`--clr-green-1`），積分數字 countup 動畫
- **排行榜**：前三名特殊大尺寸列，各列底色依學校代表色設定淡透明背景
- **LIVE 指示器**：右上角紅色閃爍圓點 + "LIVE" 文字，`animation: blink 1s step-end infinite`

**核心 JS 邏輯**：

```javascript
// 1. 從 URL 取得 session UUID
const params = new URLSearchParams(location.search);
const sessionId = params.get('session');
const sessionCode = params.get('code');

// 2. 初始載入現有數據
async function loadInitialData() {
  const { data } = await supabaseClient
    .from('daily_attempts')
    .select('student_uid, student_name, school, class, score, speech_scores, wrong, timestamp')
    .eq('session_id', sessionId)
    .order('score', { ascending: false })
    .order('timestamp', { ascending: true });

  attemptsList = data || [];
  renderAll();
}

// 3. Supabase Realtime 訂閱（複用 teacher.html 已驗證模式）
function subscribeRealtime() {
  supabaseClient
    .channel(`scoreboard-${sessionId}`)
    .on('postgres_changes',
      { event: '*', schema: 'public', table: 'daily_attempts' },
      payload => {
        const record = payload.new;
        if (!record || record.session_id !== sessionId) return;
        const idx = attemptsList.findIndex(a => a.student_uid === record.student_uid);
        if (idx >= 0) {
          // 更新現有（同學生重複提交取最高分）
          if (record.score >= attemptsList[idx].score) {
            attemptsList[idx] = record;
          }
        } else {
          attemptsList.push(record);
          playEntrySound(); // Web Audio 合成入場音效
          showEntryFlash(record.student_name); // 全畫面閃光特效
        }
        attemptsList.sort((a, b) => b.score - a.score || new Date(a.timestamp) - new Date(b.timestamp));
        renderAll();
      })
    .subscribe(status => {
      updateConnectionStatus(status === 'SUBSCRIBED');
    });
}

// 4. 三校積分加總（直接從 attemptsList 計算）
function computeSchoolScores() {
  const schools = {
    '新港國小': { total: 0, count: 0 },
    '鳳岡國小': { total: 0, count: 0 },
    '豐田國小': { total: 0, count: 0 },
  };
  attemptsList.forEach(a => {
    if (schools[a.school]) {
      schools[a.school].total += a.score;
      schools[a.school].count += 1;
    }
  });
  return schools;
}

// 5. Countup 動畫（積分數字平滑滾動）
function animateCount(el, from, to, duration = 800) {
  const start = performance.now();
  function update(now) {
    const progress = Math.min((now - start) / duration, 1);
    const eased = 1 - Math.pow(1 - progress, 3); // ease-out cubic
    el.textContent = Math.round(from + (to - from) * eased);
    if (progress < 1) requestAnimationFrame(update);
  }
  requestAnimationFrame(update);
}

// 6. 教師控制：關閉對抗賽（僅顯示給 teacher_uid 匹配的教師）
async function endContest() {
  if (!confirm('確定要結束本場對抗賽嗎？')) return;
  await supabaseClient.from('challenge_sessions')
    .update({ status: 'closed' })
    .eq('id', sessionId);
  showGameOverScreen();
}
```

**教師控制權驗證方式**：

```javascript
// scoreboard.html 初始化時
async function checkTeacherControl() {
  const { data: { user } } = await supabaseClient.auth.getUser();
  if (!user) return; // anon 訪客看不到控制按鈕

  const { data: session } = await supabaseClient
    .from('challenge_sessions')
    .select('teacher_uid')
    .eq('id', sessionId)
    .single();

  if (session && session.teacher_uid === user.id) {
    document.getElementById('teacher-controls').style.display = 'flex';
  }
}
```

**場次結束動畫**：

場次 `status` 被設為 `closed` 時（透過 Realtime 偵測或手動觸發），顯示：
- 全畫面「🏆 競賽結束！」頒獎台畫面
- 播放 Web Audio 合成獲勝音效（C5→E5→G5→C6 滑升音）
- 40 個寶石彩帶 Confetti 動畫（複用 `js/challenge.js` 的 `createGemConfetti` 邏輯）
- 前三名放大顯示於螢幕中央

---

#### [MODIFY] `js/config.js`

```javascript
// 更新版本號
const APP_VERSION = 'v1.7.0-CP26'
```

---

### 六、開發前置確認清單

- [ ] 確認 Supabase anon RLS 已套用（`challenge_sessions` 允許 anon 讀取 active sessions）
- [ ] 本機執行 `npx.cmd supabase db push --dry-run` 確認 migration 無衝突
- [ ] 確認 `teacher.html` 的 `activeSession.id` 變數名稱（需看 JS 中實際保存進行中場次的變數名）

---

### 七、逐步實作順序

> 建議按以下順序實作，每完成一步就做一次 `npm.cmd run check` 確保語法正確：

1. **Step 1**（10 min）：撰寫 SQL Migration，在 Supabase SQL Editor 套用 + 寫入 migration 檔案
2. **Step 2**（10 min）：修改 `teacher.html`，加入「📺 開啟大螢幕看板」按鈕與 `openScoreboard()` 函式
3. **Step 3**（10 min）：修改 `index.html`，加入 `#live-contest-banner` HTML 結構與 CSS 動畫
4. **Step 4**（10 min）：修改 `js/index.js`，加入 `checkLiveContest()` 函式
5. **Step 5**（40 min）：建立 `scoreboard.html`（頁面結構 + CSS 全螢幕深色主題）
6. **Step 6**（20 min）：`scoreboard.html` JS：初始載入 + Realtime 訂閱 + 排行榜渲染
7. **Step 7**（15 min）：三校積分 Panel + Countup 動畫
8. **Step 8**（10 min）：新生入榜特效（滑入動畫 + 音效）+ 動態牆
9. **Step 9**（10 min）：場次結束動畫（頒獎台 + Confetti）+ 教師控制按鈕
10. **Step 10**（5 min）：更新 `js/config.js` 版本號 → `npm.cmd run check` → `npm.cmd run build:pages` → `git push`

---

### 八、驗收標準

- [ ] `npm.cmd run check` 通過（JS 語法 + Inline scripts + JSON 全綠）
- [ ] `npm.cmd run build:pages` 通過，`dist/scoreboard.html` 成功產出
- [ ] 教師在 `teacher.html` 建立課堂場次後，「📺 開啟大螢幕看板」按鈕出現
- [ ] 點擊按鈕後，新分頁正確開啟 `scoreboard.html?session=UUID&code=XXXXXX`
- [ ] 看板顯示等待畫面（尚無學生提交時）
- [ ] 學生在 `class.html` 加入，完成挑戰後提交，看板無需刷新即時出現學生名字
- [ ] 三校積分 Panel 正確按 school 分組加總
- [ ] 前三名正確顯示 🥇🥈🥉 勳章
- [ ] `index.html` 在對抗賽進行中出現「🏆 三校即時對抗賽進行中！」橫幅
- [ ] 橫幅連結點擊後正確開啟看板
- [ ] 教師關閉場次後，看板顯示結束動畫，橫幅消失（下次 index.js 查詢結果為空）
- [ ] 已 push 至 GitHub，GitHub Actions 部署成功

---

### 九、已知限制與後續建議

1. **首頁大廳橫幅延遲**：`checkLiveContest()` 為一次性查詢，若對抗賽在學生瀏覽首頁後才開始，學生需手動刷新頁面才看到橫幅（可後續升級為首頁也訂閱 Realtime）
2. **多場次同時開啟**：目前查詢 `challenge_sessions` 只取第一筆 active 場次，若有多位老師同時開啟不同場次，首頁只顯示一個橫幅
3. **看板關閉場次後橫幅自動消失**：由於首頁無 Realtime 監聽 `challenge_sessions`，橫幅需等下次頁面重整才消失（可接受）
4. **Scoreboard 在不同裝置開啟**：若教師在另一台電腦上輸入 scoreboard URL，因無教師帳號 Session，只能「觀看」但無法點擊「結束對抗賽」按鈕（這是正確設計）

---

